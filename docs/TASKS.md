# PointStash — Task Queue

The **daily routine** takes the **top unblocked** unchecked task, implements it on a
`routine/<slug>` branch, and opens a PR.

Format: `- [ ] <title> — <acceptance criteria>`
- Top = highest priority. A `> blocked: <reason>` line skips a task until cleared.
- The routine ticks `[x]` inside its PR.

## Queue

- [ ] Implement `POST /api/accounts/[id]/redeem` — accepts `{ redemptionOptionId }`, verifies account ownership (404 on missing/other-user), verifies option belongs to the same chain and user has sufficient points (400 otherwise), atomically deducts `pointsCost` from `currentPoints` and inserts a `PointsHistory` row with `changeReason: "REDEMPTION"`; returns `{ account, pointsHistory }`; add unit tests in `tests/unit/api-accounts-redeem.test.ts`. See design in `docs/DECISIONS.md` (2026-06-16).
- [ ] Playwright E2E for the redeem flow — sign in (seeded/MSW), view a deal, complete a redeem, see the balance update.
  > blocked: needs `POST /api/accounts/[id]/redeem` route + UI affordance to land first (see task above).
- [x] Harden `isCronRequest` against an unset `CRON_SECRET` — `src/lib/api.ts` builds `Bearer ${process.env.CRON_SECRET}`, so if the env var is ever unset a literal `Authorization: Bearer undefined` header passes. Reject when `CRON_SECRET` is empty/undefined regardless of header value; add a regression test in `tests/unit/api-guards.test.ts`.
- [x] Architect: design a redemption-completion flow — decompose "user marks a redemption option as redeemed, the linked account's points balance is deducted by `pointsCost`, and a `PointsHistory` entry is recorded" into implementable tasks (schema check, API route, UI affordance on `RedeemPage`/`DealCard`). Record the design in `docs/DECISIONS.md` and leave a follow-up implementation task at the top of the queue. Unblocks the redeem-flow E2E above.
- [x] Fix lint warnings in `src/lib/connectors/base.ts` — resolve the 4 `@typescript-eslint/no-unused-vars` warnings on the `_credentials`/`_token` stub parameters (e.g. an eslint override for this file, or restructure the stub signatures) without changing `BaseConnector`'s public contract; `npm run lint` reports zero warnings on this file.
- [x] Unit tests for the connectors registry (`src/lib/connectors/`) — `getConnector` returns the right connector per chain slug and `undefined` for unknown slugs; `hasImplementedConnector` reflects each connector's `implemented` flag; every unimplemented connector's `authenticate`/`getPointsBalance`/`getRecentTransactions`/`refreshToken` reject with `NotImplementedError`.
- [x] API tests for `/api/accounts/[id]` edge cases — `PUT`/`DELETE` return 404 (not 403, to avoid leaking existence) for an account belonging to another user; invalid/non-numeric `currentPoints` is rejected with 400; verifies per-user scoping in the underlying query.
- [x] Unit tests for `src/lib/deal-reminder-email.ts` — subject/body rendering for 0, 1, and many upcoming-expiry deals (pluralization), and deals across multiple chains in one digest.
- [x] Unit tests for `src/lib/extension-auth.ts` and `src/lib/extension-bridge.ts` — pairing-token issuance, expiry, and rejection of malformed/expired/already-used tokens.
- [x] Unit tests for `src/lib/points-history.ts` — series aggregation across a date range with gaps (no recorded points on some days), multiple accounts for the same user, and an account with a single data point.
- [ ] Edge-case unit tests for `src/lib/deal-reminders.ts` — a deal expiring exactly at the reminder cutoff boundary (72 h inclusive), a deal where `expiresAt` is null at runtime (should return false, not throw — add a null guard to `isExpiringSoon`), and a deal that already has a reminder recorded (no duplicate send).
- [ ] Accessibility pass on icon-only dashboard buttons — audit `ChainAccountCard`, `DealCard`, and `Sidebar`/`MobileNav` for icon-only buttons missing `aria-label` (e.g. unlink, redeem, nav icons); add labels so each is announced correctly by screen readers, with a test asserting accessible names via `getByRole`.
- [ ] Playwright E2E for the OCR screenshot upload flow — on `/dashboard/accounts`, upload a fixture screenshot via `AddAccountModal`'s screenshot method, confirm the OCR-extracted point total pre-fills the balance field, and saving updates the account's displayed balance.
- [ ] DX: add a Husky + lint-staged pre-commit hook — run `npm run lint` and `npm run typecheck` (or an eslint/tsc subset) on staged files before commit; document setup in `docs/DEPLOYMENT.md` or a new `docs/DX.md`.
- [x] Fix 403→404 existence-disclosure in `GET /api/points/history?accountId` — the route calls `db.account.findUnique({ where: { id: accountId } })` then returns 403 if `userId` mismatches, leaking that the account exists; change to `db.account.findFirst({ where: { id: accountId, userId: guard.userId } })` returning 404 on null; add regression tests to `tests/unit/api-points.test.ts` verifying that another user's `accountId` returns 404, not 403.
- [x] `requireAdmin` unit tests — extend `tests/unit/api-guards.test.ts`: returns 401 when there is no session; returns 403 when the session user's email is not in `ADMIN_EMAIL`; returns 403 (fails closed) when `ADMIN_EMAIL` is empty or unset; accepts the correct admin email case-insensitively; accepts one of a comma-separated list of admin emails.
- [ ] Unit tests for `mintExtensionToken` and `hashExtensionToken` in `src/lib/extension-auth.ts` — in new `tests/unit/extension-auth-lib.test.ts`: `mintExtensionToken` in non-production returns a string starting with `ps_dev_`; token contains only URL-safe characters (no `+`, `/`, `=`); `hashExtensionToken` returns a 64-char lowercase hex string; the same input always produces the same hash; two different tokens produce different hashes.
- [x] Unit tests for `chainHasExtensionSupport` and `EXTENSION_SUPPORTED_CHAINS` in `src/lib/extension-bridge.ts` — `chainHasExtensionSupport` returns true for each of the 14 slugs; returns false for an unknown slug and for empty string; `EXTENSION_SUPPORTED_CHAINS.size` is exactly 14.
- [x] Unit tests for `verifyUnsubscribeToken` in `src/lib/unsubscribe-token.ts` — extend `tests/unit/unsubscribe-token.test.ts`: round-trip returns the original `userId`; wrong secret returns null; truncated token returns null; empty string returns null; a tampered payload returns null.
- [x] Unit tests for `parseIntParam` in `src/lib/api.ts` — add `tests/unit/parse-int-param.test.ts`: valid integer string returns parsed value; negative string falls back to the default; NaN / non-numeric string falls back; null/empty falls back to default; a value above `max` is clamped to `max`; zero is accepted and not treated as falsy.
- [ ] Unit tests for `estimatedDealValueCents` and `totalEstimatedDollars` in `src/lib/dashboard.ts` — extend `tests/unit/dashboard-deal-value.test.ts` with `totalEstimatedDollars` tests (the function exists but is untested): sums multiple accounts' values; a zero-balance account contributes 0; an account for a chain with no redemption contributes 0. (`estimatedDealValueCents` is already tested in that file.)
- [ ] API integration test for `GET /api/cron/deal-reminders` — extend `tests/unit/api-deal-reminders-cron.test.ts`: with a valid header and no opted-in users returns `{ ok: true, candidates: 0, sent: 0, logged: 0, errors: [] }`; a per-user email-send error is captured in `errors[]` without aborting the batch (other users still get emailed).
- [ ] Unit tests for `bestRedemptionFor` and `bestRedemptionLabel` in `src/lib/dashboard.ts` — in new `tests/unit/dashboard-best-redemption.test.ts`: `bestRedemptionFor` returns the highest `centsPerPoint` among matching chainId redemptions; returns null when no redemptions match the chainId; handles multiple redemptions with same centsPerPoint; `bestRedemptionLabel` formats as `"ItemName · N pts"`; returns `"Linking redemptions…"` when passed null.
- [ ] Unit tests for `hashResetToken`, `mintResetToken`, and `resetTokenExpiry` in `src/lib/reset-tokens.ts` — in new `tests/unit/reset-tokens.test.ts`: `hashResetToken` returns a 64-char lowercase hex string; same input always produces the same hash; different inputs produce different hashes; `mintResetToken` produces a URL-safe string of at least 40 chars; `resetTokenExpiry` returns a date approximately 1 hour in the future (within ±5 s of `Date.now() + RESET_TOKEN_TTL_MS`).
- [ ] `GET /api/points/history` pagination tests — extend `tests/unit/api-points.test.ts`: `?limit=10` passes `take: 10` to the Prisma query; `?limit=250` is clamped to 200 (the configured max); `?offset=20` passes `skip: 20`; no `accountId` param omits the `accountId` filter from `where`.
- [ ] Admin deal routes access-control tests — extend `tests/unit/api-admin-deals.test.ts`: `GET /api/admin/deals` returns 401 when unauthenticated; returns 403 when authenticated but not an admin email; returns 200 with deal list for a valid admin; `POST /api/admin/deals` 403s a non-admin; `DELETE /api/admin/deals/[id]` 403s a non-admin.
- [ ] `isExpiringSoon` boundary + null-safety tests — add to `tests/unit/deal-reminders.test.ts` and add a null guard to `src/lib/deal-reminders.ts` `isExpiringSoon`: a deal expiring exactly 72 h from now is `true` (boundary is inclusive); a deal where `expiresAt` is coerced to null at runtime returns `false` without throwing.
- [ ] Redemption UI affordance on `RedeemPage` / `DealCard` — add "Mark as Redeemed" button (secondary, below the existing external-link CTA) that opens a confirmation sheet showing item name + points cost + resulting balance, calls `POST /api/accounts/[id]/redeem`, then invalidates `useAccounts` + `usePoints` SWR keys on success; add a Vitest component test asserting the button is present and the confirmation renders.

## Done
<!-- routine PRs move completed items here -->

- [x] Architect: design a redemption-completion flow — design recorded in `docs/DECISIONS.md` (2026-06-16); `POST /api/accounts/[id]/redeem` impl task added to top of queue; E2E blocked task updated. (PR #48)
- [x] Unit tests for `src/lib/extension-auth.ts` and `src/lib/extension-bridge.ts` — `requireExtensionAuth` guard tests + `chainHasExtensionSupport`/`EXTENSION_SUPPORTED_CHAINS` in `tests/unit/extension-auth-unit.test.ts`. (PR #49)
- [x] Fix 403→404 existence-disclosure in `GET /api/points/history?accountId` — switched to `findFirst({ id, userId })`; regression test in `tests/unit/api-points.test.ts`. (PR #50)
- [x] `requireAdmin` unit tests — 12 tests added to `tests/unit/api-guards.test.ts`. (PR #51)
- [x] Unit tests for `parseIntParam` in `src/lib/api.ts` — 10 tests in `tests/unit/parse-int-param.test.ts`. (PR #52)
- [x] Harden `isCronRequest` against an unset `CRON_SECRET` — already implemented: `if (!secret) return false` guard + regression tests for `"Bearer undefined"` and empty-string secret in `tests/unit/api-guards.test.ts`.
- [x] Fix lint warnings in `src/lib/connectors/base.ts` — already resolved: `/* eslint-disable @typescript-eslint/no-unused-vars */` comment at file top; `npm run lint` reports zero warnings.
- [x] API tests for `/api/accounts/[id]` edge cases — already covered in `tests/unit/api-accounts.test.ts`: 404 for cross-user PUT/DELETE, 400 for invalid points, scoped `findFirst` assertions.
- [x] Unit tests for `src/lib/deal-reminder-email.ts` — already covered in `tests/unit/deal-reminder-email.test.ts`: 0/1/many-deal subjects, multi-chain digest, name/no-name greeting, unsubscribe URL handling.
- [x] Unit tests for `src/lib/points-history.ts` — already covered in `tests/unit/points-history-series.test.ts`: empty history, chronological ordering, anchor point, immutability.
- [x] Unit tests for `chainHasExtensionSupport` and `EXTENSION_SUPPORTED_CHAINS` in `src/lib/extension-bridge.ts` — covered in `tests/unit/extension-auth-unit.test.ts` (PR #49).
- [x] Unit tests for `verifyUnsubscribeToken` in `src/lib/unsubscribe-token.ts` — already covered in `tests/unit/unsubscribe-token.test.ts`: round-trip, wrong secret, tampered payload, malformed tokens.
- [x] Unit tests for the connectors registry (`src/lib/connectors/`) — `getConnector` returns the right connector per chain slug and `undefined` for unknown slugs; `hasImplementedConnector` reflects each connector's `implemented` flag; every unimplemented connector's `authenticate`/`getPointsBalance`/`getRecentTransactions`/`refreshToken` reject with `NotImplementedError`.
- [x] Add CI workflow `.github/workflows/ci.yml` — `npm ci`, `npm run typecheck`, `npm run lint`, `npm run test:run` on PRs to main; green on default branch.
- [x] OCR edge-case tests in `src/lib/ocr.ts` — common OCR artifacts are corrected per chain; malformed input fails gracefully (no throw).
- [x] API auth/cron guard tests — `requireAuth` rejects unauthenticated calls; `isCronRequest` only accepts the configured cron secret.
- [x] Cents-per-point ranking unit tests — given balances + redemption options, the highest cents-per-point redemption is surfaced first; ties and zero-point cases handled.
- [x] Unit tests for the connectors registry (`src/lib/connectors/`) — `getConnector` returns the right connector per chain slug and `undefined` for unknown slugs; `hasImplementedConnector` reflects each connector's `implemented` flag; every unimplemented connector's `authenticate`/`getPointsBalance`/`getRecentTransactions`/`refreshToken` reject with `NotImplementedError`.
