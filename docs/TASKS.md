# PointStash — Task Queue

The **daily routine** takes the **top unblocked** unchecked task, implements it on a
`routine/<slug>` branch, and opens a PR.

Format: `- [ ] <title> — <acceptance criteria>`
- Top = highest priority. A `> blocked: <reason>` line skips a task until cleared.
- The routine ticks `[x]` inside its PR.

## Queue

- [ ] Affordable-redemption-alert: schema migration — per the 2026-06-19 design in `docs/DECISIONS.md`, add `User.notifyAffordable Boolean @default(true)` and a new `AffordabilityAlert` model (`{ id, accountId, userId, redemptionOptionId, sentAt }`, `@@unique([accountId, redemptionOptionId])`, `@@index([userId])`, cascading FKs to `Account`/`User`/`RedemptionOption`); generate the Prisma migration; keep `prisma/seed.ts` idempotent.
- [ ] Affordable-redemption-alert: detection helpers — `src/lib/affordable-alerts.ts`, mirroring `src/lib/deal-reminders.ts`'s shape: given linked accounts, redemption options, and already-alerted `(accountId, redemptionOptionId)` keys, return the set of accounts that have newly crossed into affording their chain's best redemption (reuse `bestRedemptionFor` from `src/lib/dashboard.ts`); unit tests covering not-yet-affordable, newly-affordable, and already-alerted (no repeat) cases.
- [ ] Affordable-redemption-alert: email — `src/lib/affordable-alert-email.ts` with `sendAffordableRedemptionEmail`, mirroring `src/lib/deal-reminder-email.ts`'s Resend send-or-log-in-dev pattern and unsubscribe-link reuse, but with "you can now afford X" framing (not the expiry framing); unit tests for the dev-log/production-throw/Resend-error/happy-path cases.
- [ ] Affordable-redemption-alert: cron route — `GET /api/cron/affordable-redemptions`, cron-secret guarded (`isCronRequest`), wiring the detection helper + email together and recording `AffordabilityAlert` rows; returns `{ ok, candidates, sent, logged, errors }` like `/api/cron/deal-reminders`; register it in `vercel.json`'s `crons` array; integration tests mirroring `tests/unit/api-deal-reminders-cron.test.ts`.
- [ ] Affordable-redemption-alert: Settings UI toggle — add an "Affordable redemption alerts" switch wired to `notifyAffordable` next to the existing `notifyExpiring`/`notifyDeals`/`notifyDigest` toggles in `src/app/dashboard/settings/page.tsx`.
- [ ] Playwright E2E for the redeem flow — sign in (seeded), view a deal, complete a redeem via `RedemptionTable`'s "Mark redeemed" confirm dialog (merged in #67), see the balance update.
  > blocked: same root cause as the OCR-upload E2E below — no `DATABASE_URL`/seeded Postgres dev DB is available in the routine's sandbox and `.github/workflows/ci.yml` doesn't run `test:e2e`, so a real Playwright run can't be driven or verified end-to-end here. The UI affordance itself is no longer the blocker (merged to main); only the missing E2E test-infra is.
- [ ] Playwright E2E for the OCR screenshot upload flow — on `/dashboard/accounts`, upload a fixture screenshot via `AddAccountModal`'s screenshot method, confirm the OCR-extracted point total pre-fills the balance field, and saving updates the account's displayed balance.
  > blocked: no `DATABASE_URL`/Postgres instance is available in the routine's sandbox and `.github/workflows/ci.yml` doesn't run `test:e2e`, so a real Playwright run against `/dashboard/accounts` can't be driven or verified end-to-end here. Needs a provisioned test DB (or a documented Postgres-less fixture strategy) wired into CI before this can be implemented and checked green. In the meantime, see the new component-level `ScreenshotUploader` test task below, which covers the same OCR-confirm UX without a live DB.
- [ ] Fix per-chain error isolation in deal-scraping routes — `GET /api/cron/scrape-deals` and `POST /api/deals/scrape` (`src/app/api/cron/scrape-deals/route.ts`, `src/app/api/deals/scrape/route.ts`) duplicate an identical scan loop, and neither isolates `replaceAutoDeals` failures: if it throws for one chain (e.g. a transient DB error), the whole request 500s and aborts before scanning any remaining chains, even though `deactivateExpiredDeals` and earlier chains already succeeded. Extract the shared "scrape + replace" loop into `src/lib/deals.ts` (e.g. `scanAndReplaceDeals(chains)`), wrap each chain's `replaceAutoDeals` call in its own try/catch (mirroring the existing `outcome.ok` isolation and the per-user isolation already used in `cron/deal-reminders`), and have both routes call it. Add a regression test asserting one chain's `replaceAutoDeals` throwing doesn't prevent the next chain from being scanned/replaced.
- [ ] Return 400 (not 500) for a corrupt or spoofed image upload in `POST /api/upload` — `src/app/api/upload/route.ts` checks `file.type.startsWith("image/")` but never validates the actual bytes; if `sharp(original)` fails to decode a file with a spoofed/corrupted `image/*` content-type, the failure bubbles to the route's generic catch and returns a 500 ("Failed to process upload") for what is actually invalid client input. Catch the `sharp()` call specifically and return 400 ("Invalid image data") instead; add a regression test in `tests/unit/api-upload.test.ts` using a fixture of non-image bytes with an `image/png` content type.
- [ ] Add standard security response headers — `next.config.ts` has no `headers()` config, so the app ships none of `X-Content-Type-Options`, `X-Frame-Options`/`frame-ancestors`, `Referrer-Policy`, or `Permissions-Policy`. Add an async `headers()` function applying `X-Content-Type-Options: nosniff`, `X-Frame-Options: DENY`, `Referrer-Policy: strict-origin-when-cross-origin`, and a conservative `Permissions-Policy` to all routes; add a test asserting the exported config's headers list includes each one.
- [ ] Rate-limit `POST /api/auth/signup` by IP — unlike `forgot-password` (capped per-email-per-hour), signup has no rate limiting at all, so a single client can mass-create accounts with no friction. Add an IP-scoped cap (new `SignupAttempt` Prisma model — hashed IP + `createdAt`, mirroring the `PasswordResetToken` counting pattern — via a migration), capping at N (e.g. 5) signups per IP per hour and returning 429 once exceeded (no enumeration concern here, unlike forgot-password, so the cap can be disclosed in the response). Extend `tests/unit/api-auth-signup.test.ts` with capped/under-cap/scoping cases.
- [ ] Bring `docs/API.md` up to date with the real route list — it documents 9 endpoints but `src/app/api/**/route.ts` has grown to 23; missing entirely: `POST /api/auth/signup`, `POST /api/auth/forgot-password`, `POST /api/auth/reset-password`, `POST /api/accounts/:id/redeem`, `GET/PATCH/DELETE /api/admin/deals` (+`/:id`), `GET/POST /api/extension/pair`, `POST /api/extension/sync`, `POST /api/extension/sync-offers`, `GET /api/extension/whoami`, `GET /api/unsubscribe`, `GET /api/user/me`, and `GET /api/cron/deal-reminders`. Audit every route file and add a one-line entry per endpoint (method, path, auth requirement, purpose) in the existing format.
- [ ] Add `aria-current="page"` to the active dashboard nav link — `Sidebar.tsx` and `MobileNav.tsx` (`src/components/dashboard/Sidebar.tsx`, `src/components/layout/MobileNav.tsx`) indicate the active route only via background color/animation, with no `aria-current` attribute, so screen-reader users navigating the sidebar/mobile menu have no way to tell which page is active. Add `aria-current={active ? "page" : undefined}` to `Sidebar`'s computed-active `Link`, and compute the equivalent active state in `MobileNav` (it currently doesn't read `pathname` at all); add a test asserting `getByRole("link", { current: "page" })` resolves to the expected item per route.

## Done
<!-- routine PRs move completed items here -->

- [x] Unit tests for `replaceAutoDeals` in `src/lib/deals.ts` — mocks `db.deal.deleteMany`/`createMany`/`$transaction` in `tests/unit/deals.test.ts`; asserts the chain/auto-source scoping, `deleteMany` runs before `createMany`, zero deals still clears existing auto deals, and the return value equals the row count.
- [x] Add CI workflow `.github/workflows/ci.yml` — `npm ci`, `npm run typecheck`, `npm run lint`, `npm run test:run` on PRs to main; green on default branch.
- [x] OCR edge-case tests in `src/lib/ocr.ts` — common OCR artifacts are corrected per chain; malformed input fails gracefully (no throw).
- [x] API auth/cron guard tests — `requireAuth` rejects unauthenticated calls; `isCronRequest` only accepts the configured cron secret.
- [x] Cents-per-point ranking unit tests — given balances + redemption options, the highest cents-per-point redemption is surfaced first; ties and zero-point cases handled.
- [x] Unit tests for the connectors registry (`src/lib/connectors/`) — `getConnector` returns the right connector per chain slug and `undefined` for unknown slugs; `hasImplementedConnector` reflects each connector's `implemented` flag; every unimplemented connector's `authenticate`/`getPointsBalance`/`getRecentTransactions`/`refreshToken` reject with `NotImplementedError`.
- [x] Harden `isCronRequest` against an unset `CRON_SECRET` — rejects empty/undefined `CRON_SECRET` regardless of header value; regression coverage in `tests/unit/api-guards.test.ts`.
- [x] Fix lint warnings in `src/lib/connectors/base.ts` — eslint-disable scoped to the stub file's intentionally-unused parameters; `npm run lint` is clean.
- [x] API tests for `/api/accounts/[id]` edge cases — `PUT`/`DELETE` return 404 (not 403) for a cross-user account; invalid/non-numeric `currentPoints` rejected with 400; per-user scoping verified in `tests/unit/api-accounts.test.ts`.
- [x] Unit tests for `src/lib/deal-reminder-email.ts` — subject/body rendering for 0, 1, and many upcoming-expiry deals (pluralization), and multi-chain digests, in `tests/unit/deal-reminder-email.test.ts`.
- [x] Unit tests for `src/lib/extension-auth.ts` and `src/lib/extension-bridge.ts` — pairing-token issuance and rejection of malformed/revoked tokens in `tests/unit/extension-auth-unit.test.ts` and `tests/unit/api-extension-pair.test.ts`.
- [x] Unit tests for `src/lib/points-history.ts` — `buildBalanceSeries` covered for empty input, chronological ordering, single-change anchoring, and no-mutation in `tests/unit/points-history-series.test.ts`.
- [x] Edge-case unit tests for `src/lib/deal-reminders.ts` — exact reminder-cutoff boundary, missing `expiresAt`, and already-reminded (no duplicate) cases in `tests/unit/deal-reminders.test.ts`.
- [x] Fix 403→404 existence-disclosure in `GET /api/points/history?accountId` — uses `db.account.findFirst({ where: { id, userId } })`; regression test in `tests/unit/api-points.test.ts` verifies another user's `accountId` returns 404, not 403.
- [x] `requireAdmin` unit tests — 401 with no session, 403 for a non-admin email, fail-closed 403 for an empty/unset `ADMIN_EMAIL`, case-insensitive match, and comma-separated-list support, in `tests/unit/api-guards.test.ts`.
- [x] Unit tests for `mintExtensionToken` and `hashExtensionToken` in `src/lib/extension-auth.ts` — `tests/unit/extension-auth-lib.test.ts`.
- [x] Unit tests for `verifyUnsubscribeToken` in `src/lib/unsubscribe-token.ts` — round-trip, wrong-secret, truncated, empty, and tampered-payload cases in `tests/unit/unsubscribe-token.test.ts`.
- [x] Unit tests for `parseIntParam` in `src/lib/api.ts` — `tests/unit/parse-int-param.test.ts`.
- [x] Unit tests for `estimatedDealValueCents` and `totalEstimatedDollars` in `src/lib/dashboard.ts` — `tests/unit/dashboard-deal-value.test.ts`.
- [x] API integration test for `GET /api/cron/deal-reminders` — cron-secret guard, zero-candidate stats shape, and per-user send errors captured without aborting the batch, in `tests/unit/api-deal-reminders-cron.test.ts`.
- [x] Unit tests for `affordableDealCount` in `src/lib/dashboard.ts` — `tests/unit/dashboard-affordable-count.test.ts`.
- [x] API integration test for `GET /api/cron/scrape-deals` — cron-secret guard, per-chain error isolation, and `chainsScanned`/`dealsInserted`/`dealsDeactivated` in the response, in `tests/unit/api-cron-scrape-deals.test.ts`.
- [x] CI: add `prisma generate` step before typecheck and tests in `.github/workflows/ci.yml` so fresh CI runs don't fail with `Module '"@prisma/client"' has no exported member` errors.
- [x] Rate-limit password-reset email sends — `POST /api/auth/forgot-password` skips minting/sending (still returns `{ ok: true }`) once a user has 3+ tokens created in the last hour; tests in `tests/unit/api-auth-password.test.ts`.
- [x] Architect: design an "affordable redemption alert" feature — recorded in `docs/DECISIONS.md` ("2026-06-19 — Affordable-redemption-alert design"); follow-up implementation tasks already in the queue above.
- [x] DX: add a Husky + lint-staged pre-commit hook — `.husky/pre-commit` + `lint-staged` config in `package.json`; documented in `docs/DEPLOYMENT.md`.
- [x] Implement `POST /api/accounts/[id]/redeem` — `src/app/api/accounts/[id]/redeem/route.ts` with ownership/chain/balance guards and atomic deduct + `PointsHistory` insert; tests in `tests/unit/api-accounts-redeem.test.ts`.
- [x] Unit tests for `mapScrapedDeal` and `deactivateExpiredDeals` in `src/lib/deals.ts` — covered in `tests/unit/deals.test.ts`.
- [x] Unit tests for `hashResetToken`, `mintResetToken`, and `resetTokenExpiry` in `src/lib/reset-tokens.ts` — `tests/unit/reset-tokens-lib.test.ts`.
- [x] Unit tests for `bestRedemptionFor` and `bestRedemptionLabel` in `src/lib/dashboard.ts` — `tests/unit/dashboard-best-redemption.test.ts`.
- [x] Unit tests for `dealTypeLabel` and `discountTypeLabel` in `src/lib/formatters.ts` — `tests/unit/formatters.test.ts`.
- [x] API integration tests for `PUT /api/points/update` — `tests/unit/api-points-update.test.ts`.
- [x] Unit tests for `sendResetEmail` in `src/lib/reset-tokens.ts` — covered in `tests/unit/reset-tokens-lib.test.ts` and `tests/unit/api-auth-password.test.ts`.
- [x] Accessibility: add `aria-label` to icon-only buttons — `tests/unit/icon-button-accessibility.test.tsx`.
- [x] Add stub `BaseConnector` entries for the 7 missing chains — `pancheros`/`dairyqueen`/`culvers`/`jimmyjohns`/`buffalowildwings`/`kfc`/`pandaexpress` all registered in `src/lib/connectors/index.ts`.
- [x] Add OCR point-pattern regexes for chains using the generic fallback — all 8 chains present in `POINTS_PATTERNS` (`src/lib/ocr.ts`).
- [x] Add a bespoke Cheerio scraper for `chipotle` — `src/lib/scrapers/chipotle.ts`, registered in `src/lib/scrapers/index.ts`, with `tests/unit/scrapers/chipotle.test.ts`.
- [x] Migrate off deprecated `next lint` — `lint` script now runs `eslint .` against `eslint.config.mjs` (flat config).
