# PointStash — Task Queue

The **daily routine** takes the **top unblocked** unchecked task, implements it on a
`routine/<slug>` branch, and opens a PR.

Format: `- [ ] <title> — <acceptance criteria>`
- Top = highest priority. A `> blocked: <reason>` line skips a task until cleared.
- The routine ticks `[x]` inside its PR.

## Queue

- [ ] Add Next.js `middleware.ts` to protect `/dashboard` routes — `src/app/dashboard/layout.tsx` has no session check at all (`src/app/dashboard/layout.tsx` is 4 lines: just wraps `children` in `DashboardShell`; no `auth()` call, no redirect). An unauthenticated user who knows the URL can visit `/dashboard` and see the shell frame (though SWR data calls 401). Add `src/middleware.ts` that calls `auth()` from `@/lib/auth` and redirects to `/login` for any request under `/dashboard/**` when there is no session; protect `/api/**` routes that aren't the auth or public endpoints with the same guard. Register the matcher in `middleware.ts`'s exported `config`. Add a unit test confirming unauthenticated requests to `/dashboard/` are redirected. Un-fixme `tests/e2e/auth.spec.ts`'s `"unauthenticated user is redirected to login"` test once the middleware exists.
- [ ] Rate-limit NextAuth credentials login — `src/lib/auth.ts`'s `authorize()` has zero brute-force throttling, unlike `/api/auth/signup` which caps to 5 attempts/IP/hour via the `SignupAttempt` model. Add a `LoginAttempt` Prisma model (same shape as `SignupAttempt`: hashed IP + `createdAt`; `@@index([ipHash, createdAt])`), a migration, and a cap of 10 failed-or-attempted logins per IP per 15 minutes in `authorize()` — return `null` (not throw) if the cap is hit. Unit tests: under-cap succeeds; at-cap returns null without checking the password; IP hash is scoped per 15-minute window.
- [ ] Fix stale fixture dates in `tests/mocks/fixtures/deals.json` — every `expiresAt` field is hardcoded to May 2026 and is now in the past (confirmed: `python3 -c "import json; d=json.load(open('tests/mocks/fixtures/deals.json')); print([x.get('expiresAt') for x in d])"` → all May 2026 dates). `timeUntil()` in `src/lib/utils.ts` returns `"Expired"` for any non-positive ms delta, so every seeded deal currently renders "Expired" under MSW dev mode and in Playwright e2e. Replace all `expiresAt` values with future dates relative to today (e.g. a mix of 14, 30, 60, 90 days out) using ISO strings. Update any test that hard-codes the old dates.
- [ ] Unit tests for `timeUntil` in `src/lib/utils.ts` — `timeUntil(date: Date|string): string` is exported and consumed in `DealCard`, `DealsCalendar`, and other components for compact expiry display (e.g. "3d", "5h", "2m") but is entirely absent from `tests/unit/utils.test.ts`. Add test cases covering: past date → `"Expired"`; sub-minute future → `"1m"` (the floor); 2–59 minute window → `"{n}m"`; 1–23 hour window → `"{n}h"`; 1+ day window → `"{n}d"`. Pass an explicit `now` reference time so the tests are deterministic.
- [ ] Un-fixme `"user can log out"` test in `tests/e2e/auth.spec.ts` — `test.fixme("user can log out", ...)` was fixme'd because the sign-out action requires a valid session. With MSW dev mode (`NEXT_PUBLIC_ENABLE_MSW=1`) already faking `GET /api/auth/session`, the sign-in step can seed a session; sign-out calls `NextAuth`'s `signOut` which hits `/api/auth/signout` — also mockable via MSW. Investigate whether the fixme is still warranted after MSW wires are in place; if not, implement the test end-to-end (sign in → verify dashboard loads → sign out → verify redirect to `/login`).
- [ ] Unit tests for `scrapeChain`'s LLM-then-Cheerio-fallback branching in `src/lib/scrapers/index.ts` — every existing test mocks `scrapeChain` itself (e.g. `vi.mock("@/lib/scrapers", () => ({ scrapeChain: vi.fn() }))`), so the real `scrapeChain` function's branching logic (try LLM first when `ANTHROPIC_API_KEY + DEAL_SOURCES[slug]` exist; on LLM failure fall through to `scrapers[slug]`; no registered scraper → `ok: false` error) has zero direct test coverage. Add unit tests with `ANTHROPIC_API_KEY` set vs. unset, a registered Cheerio scraper vs. none, and a failing LLM run that falls back cleanly. Mock `scrapeChainViaLLM` and the `scrapers` map (not `scrapeChain` itself) so the branching logic is actually exercised.
- [ ] Bespoke Cheerio scraper for `dairyqueen` — `dairyqueen` has a `DEAL_SOURCES` entry (`https://news.dairyqueen.com`) for the LLM path but no Cheerio fallback registered in `src/lib/scrapers/index.ts`'s `scrapers` map, unlike `chipotle` and (once PR #109 merges) `jimmyjohns`. Mirror `src/lib/scrapers/chipotle.ts`'s pattern: create `src/lib/scrapers/dairyqueen.ts`, target relevant HTML selectors (`article`, `[class*="deal"]`, `[class*="offer"]`, `h2`, `h3`), fall back to curated mock deals when live parse yields nothing, register in `scrapers`. Add `tests/unit/scrapers/dairyqueen.test.ts` with a fixture HTML parse test and a network-error-returns-empty-array test.
- [ ] Extend `GET /api/points/history` tests to cover pagination params — `tests/unit/api-points.test.ts` has tests for auth guarding and `?accountId=` scoping but no test verifies that `?limit=` and `?offset=` values are forwarded to `db.pointsHistory.findMany({ take, skip })`; that `limit > 200` is capped at 200 (the `parseIntParam` max arg); or that the response shape always includes `{ history, total, limit, offset }`. Add three cases: custom limit/offset are passed through; limit above 200 clamps to 200; response shape includes all four fields.
- [ ] Extend `POST /api/accounts` validation tests for `currentPoints` range — `createSchema` validates `currentPoints: z.number().int().nonnegative()` but `tests/unit/api-accounts.test.ts` never sends a negative integer, a float, or a non-number for `currentPoints`, so the 400 branch from these inputs is uncovered. Add test cases: `currentPoints: -1` → 400; `currentPoints: 1.5` → 400; `currentPoints: "500"` (string) → 400. Append to the existing `describe("POST /api/accounts", ...)` block.
- [ ] Direct unit tests for `correctOcrDigits` in `src/lib/ocr.ts` — `correctOcrDigits` is exported but only implicitly tested through `extractPointsFromText`. Its boundary conditions (correction only within numeric runs; standalone letters in words like "Points" must not be mangled; multi-character runs like `O,24O` → `0,240`; combined confusions `IlSB`) have no direct assertions. Add a `describe("correctOcrDigits")` block in `tests/unit/ocr-patterns.test.ts` covering: each letter-for-digit substitution (`O → 0`, `I/l → 1`, `S → 5`, `B → 8`); a standalone word like "Points" is unchanged; comma-separated numbers with OCR artifacts; mixed known-good + OCR-corrupted runs in the same string.
- [ ] Add `?chain=` filter to `GET /api/admin/deals` — the admin deals list at `src/app/api/admin/deals/route.ts` supports `?source=` filtering but no `?chain=` (chainSlug) filter. As the deal catalog grows the admin table becomes unwieldy when the admin is working on a single chain. Accept an optional `?chain=<slug>` param, resolve the `chainId` via `db.chain.findUnique({ where: { slug } })`, 400 if the slug is unknown, and add it to the `where` clause. Extend `tests/unit/api-admin-deals.test.ts` with: `?chain=mcdonalds` narrows results; an unknown slug → 400.
- [ ] Architect: design a cleanup strategy for unbounded `SignupAttempt` rows — `SignupAttempt` rows are inserted on every signup attempt (including blocked/rate-limited ones) but are never deleted; over time the table grows unboundedly and the `@@index([ipHash, createdAt])` TTL window queries slow down. Survey options — (a) delete rows older than 2h in the existing `/api/cron/scrape-deals` run (piggyback onto an existing cron, no `vercel.json` change), (b) a dedicated lightweight `DELETE FROM SignupAttempt WHERE createdAt < now() - interval '2 hours'` cron step — and record the chosen approach + rejection reasoning in `docs/DECISIONS.md`. Queue a follow-up implementation task.
- [ ] Docs: update "9 chain scrapers" count in `docs/ARCHITECTURE.md` — the Scraping layer description still says "9 chain scrapers" but the `scrapers` map in `src/lib/scrapers/index.ts` now has 10 entries (chipotle was added), and will have 11 once PR #109 (jimmyjohns) and more once dairyqueen lands. Update the count to "10 bespoke Cheerio scrapers (11 once jimmyjohns PR merges)" with an inline pointer to `Object.keys(scrapers).length` in `scrapers/index.ts` so it's obvious this drifts.
- [ ] Playwright E2E for the redeem flow — sign in (seeded/MSW), view a deal, complete a redeem, see the balance update.
  > blocked: the "Mark as Redeemed" UI affordance and `POST /api/accounts/[id]/redeem` are both implemented and merged to `main`. This task is being delivered by an in-flight routine PR (`routine/redeem-flow-e2e`) — leave as-is here to avoid a duplicate/conflicting Done entry; it'll tick itself off when that PR lands.
- [ ] Playwright E2E for the OCR screenshot upload flow — upload a fixture screenshot via `AddAccountModal`'s screenshot method, confirm the OCR-extracted point total pre-fills the balance field, and saving updates the account's displayed balance.
  > blocked: this task is being delivered by an in-flight routine PR (`routine/ocr-upload-e2e`) — leave as-is here to avoid a duplicate/conflicting Done entry; it'll tick itself off when that PR lands. That PR also discovered the `/dashboard/accounts` route mismatch captured below.
- [ ] Rewrite `tests/e2e/add-account.spec.ts` and the broken parts of `tests/e2e/dashboard.spec.ts` against the real `/dashboard` UI — both specs navigate to `/dashboard/accounts` and wait for a standalone "Unlink account" button, but no such route exists in the app (confirmed via `find src/app/dashboard -maxdepth 2 -type d`: only `admin`, `deals`, `redeem`, `settings`) — every test 404s. Account linking/unlinking actually lives directly on `/dashboard` via `AddAccountModal` (link) and `ChainAccountCard`'s "Account settings" button → `AccountDetailsDialog`'s "Disconnect" action (unlink, gated behind a native `window.confirm()`). Rewrite: `beforeEach` navigates to `/dashboard` and waits for the `"your stash"` heading (mirroring `tests/e2e/screenshot-upload.spec.ts`'s pattern); replace "Unlink account" clicks with opening a card's "Account settings" button then handling the `page.on("dialog")` native confirm; keep the existing manual-add, duplicate-chain-prevention, and inline-points-update assertions, adapted to the real UI. Leave the file's `test.fixme("shows confirmation when deleting an account")` fixme'd — it depends on the in-app confirm dialog described in the next task.
- [ ] Replace the native `window.confirm()` in `AccountDetailsDialog`'s `disconnect()` (`src/components/dashboard/AccountDetailsDialog.tsx`) with the app's own `Dialog` component — every other confirmation/destructive-action surface in the app (e.g. redeem confirmation) uses the in-house `Dialog`; a bare browser `confirm()` is visually inconsistent and awkward to drive deterministically in Playwright (`page.on("dialog")` works but is brittle compared to a normal `getByRole("dialog")` assertion). Add a small confirm step (chain name, "you can always relink later" copy, Cancel/Disconnect buttons) before calling `DELETE /api/accounts/:id`; un-fixme `tests/e2e/add-account.spec.ts`'s `"shows confirmation when deleting an account"` test (or the rewritten equivalent from the task above) to assert the new dialog instead of handling a native dialog event.
- [ ] Wire `AUTH_SECRET` into Playwright's dev server so credential-login e2e tests stop failing — `tests/e2e/auth.spec.ts`'s `"user can log in"` test calls NextAuth's credentials `signIn`, which throws `MissingSecret` because neither `AUTH_SECRET` nor `NEXTAUTH_SECRET` (`src/lib/auth.ts:16`: `process.env.AUTH_SECRET ?? process.env.NEXTAUTH_SECRET`) is set in the sandbox/CI environment. Add a throwaway test-only value to `playwright.config.ts`'s `webServer.env` (same pattern as the existing `NEXT_PUBLIC_ENABLE_MSW: "1"`), e.g. `AUTH_SECRET: "playwright-e2e-test-secret-do-not-use-in-prod"`. Do not touch any `.env*` file — this only needs to live in the Playwright config.
- [ ] Wire `npm run test:e2e` into CI — `.github/workflows/ci.yml` only runs `test:run` (Vitest); it never exercises Playwright at all. Now that MSW dev mode (`NEXT_PUBLIC_ENABLE_MSW=1`) makes the app runnable without `DATABASE_URL`/Postgres, add a step (after `Unit tests`) that runs `npx playwright install --with-deps chromium` then `npm run test:e2e -- --project=chromium` (chromium only, to keep CI fast — firefox/webkit stay local-only for now). Cache the Playwright browser binary across runs if straightforward.
  > blocked: the "rewrite add-account.spec.ts/dashboard.spec.ts" and "wire AUTH_SECRET into Playwright" tasks above must land first, or this turns CI red on day one.
- [ ] Add per-test isolation for the MSW dev-mode mock store used by Playwright e2e — `tests/mocks/handlers.ts`'s in-memory `accounts`/`dealsList`/`redemptions`/`pointsHistory` arrays persist for the lifetime of the single `npm run dev` process that backs the whole Playwright run (unlike Vitest's `afterEach(() => resetMockStore())`), so one spec file's mutations (unlinking an account, redeeming points) can bleed into another's assertions when specs run in parallel or in sequence. Add a dev-only `POST /api/test/reset-mock-store` route (only registered when `NEXT_PUBLIC_ENABLE_MSW`/`NODE_ENV !== "production"` is true) that calls `resetMockStore()`, and call it from each e2e spec's `beforeEach` (or a shared Playwright fixture) so every spec starts from clean seed data regardless of run order.
- [ ] Add a bespoke Cheerio scraper for `jimmyjohns` — it has a real scrapeable deals page (`DEAL_SOURCES.jimmyjohns` → `https://www.jimmyjohns.com/menu/deals`) but no entry in `src/lib/scrapers/index.ts`'s `scrapers` map, so it depends entirely on the LLM extraction path (`ANTHROPIC_API_KEY`) with no Cheerio fallback. Mirror `src/lib/scrapers/chipotle.ts`'s pattern, register it in the `scrapers` map, and add a unit test with a fixture HTML page under `tests/unit/scrapers/` (see `tests/unit/scrapers/chipotle.test.ts`).
- [ ] Architect: design which remaining stub connector graduates to a real API integration first — per ROADMAP.md's "Next" item ("More chains / connectors graduating from scrape to official APIs"), `pancheros`, `culvers`, `buffalowildwings`, `kfc`, and `pandaexpress` (per `src/lib/scrapers/sources.ts`'s comment) have no reliably-scrapeable public text source — their sites are JS-only SPAs or block bots — so neither the LLM nor a Cheerio scraper can cover them; the connectors registry only has `NotImplementedError` stubs for all of them (`src/lib/connectors/`). Survey each chain's public developer/loyalty API offerings (if any), pick the best first candidate, and decompose into implementable tasks (auth flow, token storage/refresh, rate limits). Record the design in `docs/DECISIONS.md` and leave a follow-up implementation task at the top of the queue — mirror the existing "Architect: design an affordable redemption alert feature" entry's now-completed shape.
- [ ] Docs: update the "9 chains" copy in `docs/PROJECT.md` (lines 3 and 20) — `CHAINS` in `src/lib/constants.ts` has grown to 17 entries (`chipotle`, `pancheros`, `dairyqueen`, `culvers`, `jimmyjohns`, `buffalowildwings`, `kfc`, `pandaexpress` were added since that copy was written) but the docs still say "9 chains" in two places. Update both to the current count (and re-derive it from `Object.keys(CHAINS).length` in a comment so it's obvious this drifts as chains are added).

## Done
<!-- routine PRs move completed items here -->

- [x] Security: fix pre-account-takeover via unverified-email account linking — `User.emailVerified DateTime?` added via Prisma migration; `src/lib/auth-link.ts`'s `linkGoogleAccount` refuses to auto-link a Google identity onto an existing row unless `emailVerified` is set; brand-new Google sign-ins still create a row with `emailVerified = now()`; covered by `tests/unit/auth-link.test.ts`.
- [x] Affordable-redemption-alert: cron route — `GET /api/cron/affordable-redemptions` route (`src/app/api/cron/affordable-redemptions/route.ts`) is cron-secret guarded, wires `findNewlyAffordableAccounts` + `sendAffordableRedemptionEmail`, records `AffordabilityAlert` rows, returns `{ ok, candidates, sent, logged, errors }`; registered in `vercel.json`; integration tests in `tests/unit/api-affordable-redemptions-cron.test.ts`.
- [x] Affordable-redemption-alert: Settings UI toggle — "Affordable redemption alerts" switch wired to `notifyAffordable` added next to the three existing notification toggles in `src/app/dashboard/settings/page.tsx`; covered by `tests/unit/settings-page-affordable-toggle.test.tsx`.
- [x] Affordable-redemption-alert: schema migration — `User.notifyAffordable Boolean @default(true)` and the `AffordabilityAlert` model (`{ id, accountId, userId, redemptionOptionId, sentAt }`, `@@unique([accountId, redemptionOptionId])`, `@@index([userId])`, cascading FKs) landed via migration `20260620151332_add_affordability_alert`; `prisma/seed.ts` remains idempotent. — `User.notifyAffordable Boolean @default(true)` and the `AffordabilityAlert` model (`{ id, accountId, userId, redemptionOptionId, sentAt }`, `@@unique([accountId, redemptionOptionId])`, `@@index([userId])`, cascading FKs) landed via migration `20260620151332_add_affordability_alert`; `prisma/seed.ts` remains idempotent.
- [x] Affordable-redemption-alert: detection helpers — `src/lib/affordable-alerts.ts`'s `findNewlyAffordableAccounts`/`affordableAlertKey`, reusing `bestRedemptionFor`; covered by `tests/unit/affordable-alerts.test.ts` (not-yet-affordable, newly-affordable, already-alerted cases).
- [x] Affordable-redemption-alert: email — `src/lib/affordable-alert-email.ts`'s `sendAffordableRedemptionEmail`, mirroring `deal-reminder-email.ts`'s Resend send-or-log-in-dev pattern; covered by `tests/unit/affordable-alert-email.test.ts`.
- [x] Architect: design an "affordable redemption alert" feature — recorded in `docs/DECISIONS.md`'s 2026-06-19 entry; implementation tasks queued (3 of the 4 — schema/detection/email — are now also done; the cron route + Settings toggle remain in the Queue above with their stale "PR #81 open/unmerged" blocked-notes removed since that schema PR has since merged).
- [x] Implement `POST /api/accounts/[id]/redeem` — `src/app/api/accounts/[id]/redeem/route.ts` verifies account ownership (404), chain match (400), sufficient points (400), then atomically deducts `pointsCost` and inserts a `PointsHistory` row (`changeReason: "REDEMPTION"`) in a `db.$transaction`; covered by `tests/unit/api-accounts-redeem.test.ts`.
- [x] DX: add a Husky + lint-staged pre-commit hook — `.husky/pre-commit` runs `npx lint-staged` then `npm run typecheck`; documented in `docs/DEPLOYMENT.md`.
- [x] Unit tests for `mapScrapedDeal` and `deactivateExpiredDeals` in `src/lib/deals.ts` — covered in `tests/unit/deals.test.ts`.
- [x] Unit tests for `hashResetToken`, `mintResetToken`, and `resetTokenExpiry` in `src/lib/reset-tokens.ts` — `tests/unit/reset-tokens-lib.test.ts`.
- [x] Unit tests for `bestRedemptionFor` and `bestRedemptionLabel` in `src/lib/dashboard.ts` — `tests/unit/dashboard-best-redemption.test.ts`.
- [x] Unit tests for `dealTypeLabel` and `discountTypeLabel` in `src/lib/formatters.ts` — `tests/unit/formatters.test.ts`.
- [x] API integration tests for `PUT /api/points/update` — `tests/unit/api-points-update.test.ts`.
- [x] Unit tests for `sendResetEmail` in `src/lib/reset-tokens.ts` — covered across `tests/unit/api-auth-password.test.ts` and `tests/unit/reset-tokens-lib.test.ts`.
- [x] Accessibility: add `aria-label` to icon-only buttons in `ChainAccountCard` and nav components — `ChainAccountCard.tsx` (Edit points/Save/Cancel/Open rewards page/See all redemptions/Sync now/Account settings), `Sidebar.tsx` (Toggle theme/Sign out/Expand-Collapse sidebar), and `MobileNav.tsx` (Menu) all have descriptive `aria-label`s; regression-tested in `tests/unit/icon-button-accessibility.test.tsx`. (`DealCard`'s button has visible text, not icon-only — no label needed there.)
- [x] Add stub `BaseConnector` entries for the 7 chains missing from the connectors registry — `pancheros`, `dairyqueen`, `culvers`, `jimmyjohns`, `buffalowildwings`, `kfc`, and `pandaexpress` all resolve via `getConnector`; `tests/unit/connector-registry.test.ts` parametrizes over every `CHAINS` key so a future un-registered chain fails the suite.
- [x] Add OCR point-pattern regexes for chains using the generic fallback — `chipotle`, `pancheros`, `dairyqueen`, `culvers`, `jimmyjohns`, `buffalowildwings`, `kfc`, and `pandaexpress` all have chain-specific entries in `POINTS_PATTERNS` (`src/lib/ocr.ts`).
- [x] Add a bespoke Cheerio scraper for `chipotle` — `src/lib/scrapers/chipotle.ts`, registered in `src/lib/scrapers/index.ts`, tested in `tests/unit/scrapers/chipotle.test.ts`.
- [x] Migrate off deprecated `next lint` — `package.json`'s `lint` script is now `"eslint ."`.
- [x] Fix per-chain error isolation in deal-scraping routes — shared scan-and-replace loop isolates each chain's `replaceAutoDeals` failure so one chain's error doesn't abort the rest.
- [x] Return 400 (not 500) for a corrupt or spoofed image upload in `POST /api/upload` — `src/app/api/upload/route.ts` catches the `sharp()` decode call specifically and returns 400 ("Invalid image data").
- [x] Add standard security response headers — `next.config.ts` ships `X-Content-Type-Options`, `X-Frame-Options`, `Referrer-Policy`, and `Permissions-Policy` on all routes.
- [x] Bring `docs/API.md` up to date with the real route list — all 23 routes documented.
- [x] Add `aria-current="page"` to the active dashboard nav link — both `Sidebar.tsx` and `MobileNav.tsx`.
- [x] Unit tests for `replaceAutoDeals` in `src/lib/deals.ts` — mocks `db.deal.deleteMany`/`createMany`/`$transaction` in `tests/unit/deals.test.ts`; asserts the chain/auto-source scoping, `deleteMany` runs before `createMany`, zero deals still clears existing auto deals, and the return value equals the row count.
- [x] Rate-limit `POST /api/auth/signup` by IP — added a `SignupAttempt` Prisma model (hashed IP + `createdAt`, via migration `20260620153316_add_signup_attempt`), `getClientIp`/`hashClientIp` helpers in `src/lib/api.ts`, and a 5-per-IP-per-hour cap returning 429 in `src/app/api/auth/signup/route.ts`; extended `tests/unit/api-auth-signup.test.ts` with under-cap/at-cap/scoping/hash cases.
- [x] Add CI workflow `.github/workflows/ci.yml` — `npm ci`, `npm run typecheck`, `npm run lint`, `npm run test:run` on PRs to main; green on default branch.
- [x] OCR edge-case tests in `src/lib/ocr.ts` — common OCR artifacts are corrected per chain; malformed input fails gracefully (no throw).
- [x] API auth/cron guard tests — `requireAuth` rejects unauthenticated calls; `isCronRequest` only accepts the configured cron secret.
- [x] Cents-per-point ranking unit tests — given balances + redemption options, the highest cents-per-point redemption is surfaced first; ties and zero-point cases handled.
- [x] Unit tests for the connectors registry (`src/lib/connectors/`) — `getConnector` returns the right connector per chain slug and `undefined` for unknown slugs; `hasImplementedConnector` reflects each connector's `implemented` flag; every unimplemented connector's `authenticate`/`getPointsBalance`/`getRecentTransactions`/`refreshToken` reject with `NotImplementedError`.
- [x] Harden `isCronRequest` against an unset `CRON_SECRET` — rejects empty/undefined `CRON_SECRET` regardless of header value; regression coverage in `tests/unit/api-guards.test.ts`.
- [x] Fix lint warnings in `src/lib/connectors/base.ts` — eslint-disable scoped to the stub file's intentionally-unused parameters; `npm run lint` is clean.
- [x] API tests for `/api/accounts/[id]` edge cases — `PUT`/`DELETE` return 404 (not 403) for a cross-user account; invalid/non-numeric `currentPoints` rejected with 400; per-user scoping verified in `tests/unit/api-accounts.test.ts`.
- [x] Unit tests for `src/lib/deal-reminder-email.ts` — subject/body rendering for 0, 1, and many upcoming-expiry deals (pluralization), and multi-chain digests, in `tests/unit/deal-reminder-email.test.ts`.
- [x] Unit tests for `src/lib/extension-auth.ts` and `src/lib/extension-bridge.ts` — pairing-token issuance and rejection of malformed/revoked tokens in `tests/unit/extension-auth-unit.test.ts` and `tests/unit/api-extension-pair.test.ts`.
- [x] Unit tests for `src/lib/points-history.ts` — `buildBalanceSeries` covered for empty input, chronological ordering, single-change anchoring, and no-mutation in `tests/unit/points-history-series.test.ts`.
- [x] Edge-case unit tests for `src/lib/deal-reminders.ts` — exact reminder-cutoff boundary, missing `expiresAt`, and already-reminded (no duplicate) cases in `tests/unit/deal-reminders.test.ts`.
- [x] Fix 403→404 existence-disclosure in `GET /api/points/history?accountId` — uses `db.account.findFirst({ where: { id, userId } })`; regression test in `tests/unit/api-points.test.ts` verifies another user's `accountId` returns 404, not 403.
- [x] `requireAdmin` unit tests — 401 with no session, 403 for a non-admin email, fail-closed 403 for an empty/unset `ADMIN_EMAIL`, case-insensitive match, and comma-separated-list support, in `tests/unit/api-guards.test.ts`.
- [x] Unit tests for `mintExtensionToken` and `hashExtensionToken` in `src/lib/extension-auth.ts` — `tests/unit/extension-auth-lib.test.ts`.
- [x] Unit tests for `verifyUnsubscribeToken` in `src/lib/unsubscribe-token.ts` — round-trip, wrong-secret, truncated, empty, and tampered-payload cases in `tests/unit/unsubscribe-token.test.ts`.
- [x] Unit tests for `parseIntParam` in `src/lib/api.ts` — `tests/unit/parse-int-param.test.ts`.
- [x] Unit tests for `estimatedDealValueCents` and `totalEstimatedDollars` in `src/lib/dashboard.ts` — `tests/unit/dashboard-deal-value.test.ts`.
- [x] API integration test for `GET /api/cron/deal-reminders` — cron-secret guard, zero-candidate stats shape, and per-user send errors captured without aborting the batch, in `tests/unit/api-deal-reminders-cron.test.ts`.
- [x] Unit tests for `affordableDealCount` in `src/lib/dashboard.ts` — `tests/unit/dashboard-affordable-count.test.ts`.
- [x] API integration test for `GET /api/cron/scrape-deals` — cron-secret guard, per-chain error isolation, and `chainsScanned`/`dealsInserted`/`dealsDeactivated` in the response, in `tests/unit/api-cron-scrape-deals.test.ts`.
- [x] CI: add `prisma generate` step before typecheck and tests in `.github/workflows/ci.yml` so fresh CI runs don't fail with `Module '"@prisma/client"' has no exported member` errors.
- [x] Rate-limit password-reset email sends — `POST /api/auth/forgot-password` skips minting/sending (still returns `{ ok: true }`) once a user has 3+ tokens created in the last hour; tests in `tests/unit/api-auth-password.test.ts`.
