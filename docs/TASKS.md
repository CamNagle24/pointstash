# PointStash — Task Queue

The **daily routine** takes the **top unblocked** unchecked task, implements it on a
`routine/<slug>` branch, and opens a PR.

Format: `- [ ] <title> — <acceptance criteria>`
- Top = highest priority. A `> blocked: <reason>` line skips a task until cleared.
- The routine ticks `[x]` inside its PR.

## Queue

- [ ] Add CSP assertions to `tests/unit/next-config-headers.test.ts` — the headers test (`tests/unit/next-config-headers.test.ts`) only asserts `X-Content-Type-Options`, `X-Frame-Options`, `Referrer-Policy`, and `Permissions-Policy`; the `Content-Security-Policy` header added to `next.config.ts` is completely untested. Extend the test to assert: (1) a `Content-Security-Policy` header is present on `/:path*`; (2) its value includes `default-src 'self'`; (3) `frame-ancestors 'none'`; (4) `object-src 'none'`; (5) `worker-src 'self'` (for the Serwist service worker). Mirror the existing test's `headers()` call pattern — no new infra needed.
- [ ] Implement MSW-aware `authorize()` bypass so `tests/e2e/auth.spec.ts`'s `"user can log in"` test passes — per the 2026-07-03 decision in `docs/DECISIONS.md`: in `src/lib/auth.ts`'s `authorize()`, add a guard (`process.env.AUTH_BYPASS_FOR_PLAYWRIGHT === "1"` AND `process.env.NODE_ENV !== "production"`) that returns the fixture demo user (`{ id: "user_demo", email: "you@stash.it", name: "You", image: null }`) when the supplied credentials match the fixture pair (`you@stash.it` / `testpassword123`); set `AUTH_BYPASS_FOR_PLAYWRIGHT=1` and `AUTH_SECRET=playwright-e2e-test-secret-do-not-use-in-prod` in `playwright.config.ts`'s `webServer.env`; un-fixme the `"user can log in"` test in `tests/e2e/auth.spec.ts`. **Security review required** before merge: reviewer must confirm (1) the bypass uses a non-`NEXT_PUBLIC_*` env var only, (2) the `NODE_ENV !== "production"` second guard is present, and (3) no production deploy sets `AUTH_BYPASS_FOR_PLAYWRIGHT`. Do NOT touch any `.env*` file.
- [ ] Wire `npm run test:e2e` into CI — `.github/workflows/ci.yml` only runs `test:run` (Vitest); it never exercises Playwright at all. Now that MSW dev mode (`NEXT_PUBLIC_ENABLE_MSW=1`) makes the app runnable without `DATABASE_URL`/Postgres, add a step (after `Unit tests`) that runs `npx playwright install --with-deps chromium` then `npm run test:e2e -- --project=chromium` (chromium only, to keep CI fast — firefox/webkit stay local-only for now). Cache the Playwright browser binary across runs if straightforward.
  > blocked: the "rewrite add-account.spec.ts/dashboard.spec.ts" and "wire AUTH_SECRET into Playwright" tasks above must land first, or this turns CI red on day one.
- [ ] Fix stale fixture dates in `tests/mocks/fixtures/deals.json` — every entry's `expiresAt` (and `scrapedAt`) is a hardcoded absolute date in May/early-June 2026, which is now in the past; `src/lib/formatters.ts`'s `timeUntil()` returns the literal string `"Expired"` for any non-positive duration, so **every** fixture deal currently renders as "Expired" under `npm run dev` (MSW) and in any Playwright spec that asserts deal-card expiry text. Replace the hardcoded ISO strings with offsets computed relative to the current date (e.g. a small helper producing `now + N days`) so the fixture stays valid indefinitely instead of drifting stale again. Check `tests/e2e/screenshot-upload.spec.ts` and `tests/e2e/redeem-flow.spec.ts` for any assertions that assumed the old dates and adjust if needed.
- [ ] Un-fixme `tests/e2e/auth.spec.ts`'s `test.fixme("user can log out", ...)` — investigate why it's skipped (sign-out itself is wired via `next-auth/react`'s `signOut({ callbackUrl: "/login" })` in `Sidebar.tsx`); it may simply be blocked on the same missing `AUTH_SECRET` covered by the "Wire `AUTH_SECRET` into Playwright's dev server" task above (NextAuth needs the secret to sign/clear the session JWT even for sign-out, independent of the deeper Prisma/`DATABASE_URL` gap that blocks credentials *login*). Verify it actually passes once that task lands — if it needs something more, document what.
- [ ] Add unit tests for `scrapeChain`'s LLM-then-Cheerio-fallback branching in `src/lib/scrapers/index.ts` — every existing caller-side test (e.g. `tests/unit/deals.test.ts`) mocks `scrapeChain` itself via `vi.mock("@/lib/scrapers", ...)`, so the function's own logic has zero direct coverage: the `canUseLLM` gate (`ANTHROPIC_API_KEY` + `DEAL_SOURCES[slug]` both present), "a successful LLM run wins even with 0 deals," LLM-failure falls through to the registered Cheerio scraper (or mock data via `BaseScraper`), and the `withTimeout` race that surfaces a timeout error per chain without hanging the batch. Mock `scrapeChainViaLLM` and a chain's registered scraper independently (not the whole module) to exercise each branch.
- [ ] Docs: fix the stale "9 chain scrapers" line in `docs/ARCHITECTURE.md`'s Layers section — actual bespoke Cheerio scraper count has grown past 9 (10 chains have one as of this writing, soon 11 once the queued/in-flight `jimmyjohns` scraper task lands); reword to avoid a hardcoded number that drifts every time a scraper is added, similar to the fix already queued for `docs/PROJECT.md`'s "9 chains" copy above.

- [ ] Add `public/robots.txt` — the app has no `robots.txt`; by default every route (including `/api/`, `/dashboard/`, `/login`, `/signup`, `/reset-password`) is crawlable. Add a minimal `robots.txt` that allows crawling the root/marketing pages but disallows API routes and authenticated-only sections: `Disallow: /api/`, `Disallow: /dashboard/`, `Disallow: /login`, `Disallow: /signup`, `Disallow: /reset-password`.
- [ ] Prune stale `LoginAttempt` rows at `authorize()` time — `src/lib/auth-login-rate-limit.ts`'s `recordFailedLogin` inserts a `LoginAttempt` row per failed attempt but never deletes them; rows older than 15 minutes (the rate-limit window) are useless and the table grows unbounded. Add a fire-and-forget `db.loginAttempt.deleteMany({ where: { createdAt: { lt: cutoff } } })` (where `cutoff = now − 15 min`) inside `isLoginRateLimited` before the count queries — mirrors the analogous pruning strategy from the `SignupAttempt` discussion. Add a test asserting that stale rows are not counted against the cap (an IP with 10 attempts older than 15 minutes + 1 fresh one is under-cap).
- [ ] Refactor `authorize()` body into a testable `authenticateCredentials` helper — `src/lib/auth.ts`'s `authorize()` callback is embedded in the NextAuth config object and cannot be imported directly in tests; all rate-limit logic is tested via the extracted `src/lib/auth-login-rate-limit.ts` helpers, but the overall `authorize()` flow (parse → rate-limit check → db lookup → bcrypt compare → record failure) has no direct unit test. Extract the body into `src/lib/auth-credentials.ts`'s `authenticateCredentials(credentials, request)` function and call it from `authorize()`; add unit tests covering: invalid credentials shape → null; rate-limited → null; unknown email → null + records failure; wrong password → null + records failure; correct password → user object. This also makes future MSW-mode gating (see Architect task above) easier to implement safely.
- [ ] Add `?source=` filter to `GET /api/deals` — `src/app/api/deals/route.ts` supports `?chain=`, `?type=`, `?active=`, `?sort=`, `?limit=`, and `?offset=` but no `?source=` filtering; the `DealSource` enum (`MANUAL | LLM | AGGREGATOR | EXTENSION`) is already on the `Deal` model. Accept an optional `?source=<DealSource>` param, validate it against the enum (400 on unknown value), and add it to the Prisma `where` clause alongside existing filters. Add unit tests: `?source=LLM` returns only LLM deals; `?source=EXTENSION` returns only extension deals; an unknown source value → 400.
- [ ] Add `src/app/dashboard/error.tsx` error boundary — Next.js App Router uses `error.tsx` co-located with the route to catch thrown errors from server components and display a recoverable UI instead of a full crash; the `src/app/dashboard/` route group has no `error.tsx`. Add a `"use client"` boundary that renders a centered card with "Something went wrong" copy and a "Try again" button wired to Next.js's `reset` prop; mirror the visual style of the dashboard's other empty-state cards.
- [ ] Add `?chainSlug=` filter to `GET /api/admin/deals` — `src/app/api/admin/deals/route.ts` accepts `?source=` and `?limit=` query params but not `?chainSlug=`, unlike the non-admin `GET /api/deals`. Add a `chainSlug` query param that filters results by chain (`where: { chainSlug }` added to the existing Prisma query), and add a unit test asserting the filter is applied and that an unknown slug returns an empty array.
- [ ] Add branded 404 page `src/app/not-found.tsx` — Next.js App Router serves the default framework 404 for unmatched routes; add a `not-found.tsx` that renders a centered on-brand card ("Page not found", a short message, and a "Go to Dashboard" link) consistent with the dashboard's empty-state card style. No auth wrapper needed — Next.js renders `not-found.tsx` outside the layout tree for top-level 404s.
- [ ] Add `GET /api/health` healthcheck endpoint — returns `{ ok: true, version: process.env.npm_package_version, timestamp: <ISO string> }` with no auth required; useful for Vercel uptime checks and load-balancer health probes; add to `docs/API.md`; unit test asserts shape and 200 status.
- [ ] Playwright E2E for auth flows — `tests/e2e/auth.spec.ts` has two `test.fixme` items ("unauthenticated user is redirected to login" and "user can log out"); un-fixme and implement: the redirect test navigates to `/dashboard` and asserts the page lands on `/login`; the logout test signs in via MSW credentials then clicks the Sidebar sign-out button and asserts the session is cleared (redirected to `/`).
- [ ] Smarter redemption recommendations — ROADMAP.md's "Smarter redemption recommendations" item; current ranking is pure cents-per-point (`centsPerPoint` desc); add an urgency-weighted score: `score = centsPerPoint × urgencyMultiplier` where `urgencyMultiplier = 1` when `expiresAt` is null, else ramps from 1 → 3 linearly over the final 7 days before expiry; surface the top-scored redemption per chain on `DealCard`'s headline badge instead of the raw CPP leader when they differ; add unit tests for the scoring function and a `"urgency score beats CPP when expiry is near"` assertion.
- [ ] Unit tests for `POST /api/extension/sync-offers` security — `src/app/api/extension/sync-offers/route.ts` has a `hostBelongsToChain` security check that validates the submitted URL's hostname against the expected chain domain, but this logic has no dedicated unit test. Add to a `tests/unit/api-extension-sync-offers.test.ts` file: (1) a valid chain + matching hostname succeeds (200); (2) a mismatched hostname for a known chain returns 400; (3) an unknown chain slug returns 400; (4) unauthenticated request returns 401. Mirror the `vi.mock("@/lib/auth")` + `vi.mock("@/lib/db")` pattern from other route unit tests.
- [ ] `PaytronixConnector` base class in `src/lib/connectors/paytronix.ts` — per the 2026-06-29 `docs/DECISIONS.md` entry, step 2 after credentials-at-rest encryption: extract shared `authenticate`/`getPointsBalance` logic into a `PaytronixConnector extends BaseConnector` base class parametrized by `{ baseUrl: string; brandId: string }` config; `PancherosConnector extends PaytronixConnector` with its brand-specific config. Unit tests: `PaytronixConnector` requires config fields at construction (throws if missing); `PancherosConnector.implemented` stays `false` until step 7 of the graduation plan; base class methods forward correctly to the config-parameterized URL.
- [ ] `GET /api/cron/sync-accounts` sync cron — per the 2026-06-29 `docs/DECISIONS.md` graduation plan (step 5): add a cron-secret-guarded `GET /api/cron/sync-accounts` route that iterates `Account`s where `syncMethod === "API"` and `getConnector(chainSlug)?.implemented`, skips any synced within the last 6 hours, calls `connector.getPointsBalance()`, updates `Account.currentPoints`, and inserts a `PointsHistory` row (`changeReason: "SYNC"`). Register in `vercel.json`'s `crons` array. Unit tests mirroring `api-affordable-redemptions-cron.test.ts`: cron-secret guard, skip-if-recent logic, per-account error isolation so one failure doesn't abort the rest.
  > blocked: `PaytronixConnector` base class and `PancherosConnector.implemented = true` must land first.
- [ ] Docs: update stale "9 chain scrapers" and "~40 unit tests" copy in `docs/ARCHITECTURE.md` — the Layers section references "9 chain scrapers" (the `scrapers` map now registers more; count drifts every time a scraper is added — link to `docs/SCRAPER_GUIDE.md` instead of hardcoding a number) and the test-suite section cites "~40 unit tests" (the suite is now ~627 tests across 71 files — update the count and note the Vitest/Playwright split). Avoid hardcoded numbers where possible; use language like "see `scripts/` or CI output for current count."
- [ ] Add docs/API.md security note about login rate-limiting — `docs/API.md` documents `POST /api/auth/signup`'s rate-limit (5-per-IP-per-hour via `SignupAttempt`) but says nothing about the now-live login rate-limit added in PR #136 (`LoginAttempt` model, 10-per-IP and 10-per-email per 15 minutes, neutral 401 on both bad-password and rate-limit to prevent email enumeration). Add a short "Rate limiting" note under the `POST /api/auth/session` (credentials sign-in) section documenting the cap, the neutral response strategy, and that `LoginAttempt` rows record hashed IP + hashed email only.
- [ ] Add E2E test for authenticated `/dashboard` access — `tests/e2e/auth.spec.ts` now covers the unauthenticated redirect (PR #137), but has no complementary test asserting that a *signed-in* session lands on `/dashboard` and sees the shell (sidebar, chain cards, or empty-state) rather than bouncing back to `/login`. Add a `test("authenticated user can access /dashboard", ...)` that navigates with an active MSW session (the `/api/auth/session` handler already returns a valid `user_demo` session when `NEXT_PUBLIC_ENABLE_MSW=1`) and asserts the URL stays at `/dashboard` and a key landmark (e.g. the sidebar nav or the "Link your first chain" empty-state) is visible.
- [ ] Prune stale `PasswordResetToken` rows at mint time — `src/app/api/auth/forgot-password/route.ts` creates `PasswordResetToken` rows via `mintResetToken` but the table is never pruned; expired tokens (older than `PASSWORD_RESET_TOKEN_EXPIRY_MINUTES`, default 60 min) accumulate indefinitely. Add a fire-and-forget `db.passwordResetToken.deleteMany({ where: { createdAt: { lt: cutoff } } })` inside `mintResetToken` in `src/lib/reset-tokens.ts` (before minting the new token), mirroring the `LoginAttempt` pruning strategy. Add a test to `tests/unit/reset-tokens-lib.test.ts` asserting that `deleteMany` is called with the correct cutoff window on every mint invocation.
- [ ] Unit tests for `POST /api/auth/reset-password` route — `src/app/api/auth/reset-password/route.ts` validates the one-time token, rejects expired tokens, and enforces minimum password length, but `tests/unit/api-auth-password.test.ts` has no cases for this route. Add: (1) valid token + new password → 200 with token consumed (`db.passwordResetToken.delete` called); (2) expired token → 400; (3) unknown/missing token → 400; (4) password shorter than 8 chars → 400. Mirror the `vi.mock("@/lib/db")` + `vi.mock("@/lib/auth")` patterns from the existing `forgot-password` tests in the same file.
- [ ] Unit tests for `GET /api/user/me` and `PATCH /api/user/me` — `src/app/api/user/me/route.ts` has no dedicated unit test file. Add `tests/unit/api-user-me.test.ts` covering: (1) GET returns profile fields (`id`, `email`, `name`, `notifyExpiring`, `notifyDeals`, `notifyDigest`, `notifyAffordable`); (2) unauthenticated GET → 401; (3) PATCH updates a single field and returns the updated profile; (4) PATCH with an invalid field type (e.g. `notifyExpiring: "yes"`) → 400; (5) unauthenticated PATCH → 401. Use the `vi.mock("@/lib/auth")` session-mock pattern from `tests/unit/api-accounts.test.ts`.
- [ ] Add global `src/app/error.tsx` error boundary — Next.js App Router renders `src/app/error.tsx` as the top-level catch for unhandled errors in the root layout tree (distinct from `src/app/dashboard/error.tsx` which only covers the dashboard subtree); currently no such file exists and unhandled root-level errors surface the raw Next.js framework error page. Add a `"use client"` boundary that renders a full-page centered card with an "Something went wrong" heading, a short explanatory sentence, and a "Try again" button wired to the `reset` prop Next.js passes; match the visual style of the dashboard's empty-state cards. Add a Vitest component test asserting the heading and "Try again" button are rendered and that clicking "Try again" calls `reset`.
- [ ] Rate-limit `POST /api/extension/pair` per user — `src/app/api/extension/pair/route.ts` has no rate limit; a compromised or buggy dashboard session could repeatedly call it, filling the `ExtensionToken` table. Add a per-user cap (e.g. max 5 pair requests per 10 minutes) using a module-level `Map<userId, {count: number, resetAt: number}>` counter (the same pattern already used by the existing in-memory signup throttle). Return 429 on breach. Add unit tests: first call succeeds; fifth call succeeds; sixth within the same window → 429; after the window resets, the counter is cleared and a new call succeeds. Mock `Date.now` with `vi.spyOn` or `vi.useFakeTimers` to avoid real-time waits.

## Done
<!-- routine PRs move completed items here -->

- [x] Encrypt `Account.credentials` at rest — `Account.credentials Json?` (`prisma/schema.prisma:179`) is currently an unused column with no reads/writes anywhere in the codebase. Per `docs/DECISIONS.md`'s 2026-06-29 connector-graduation entry, this is the prerequisite for *any* connector (starting with `PancherosConnector`/Paytronix) going live: add an `encrypt`/`decrypt` helper (AES-GCM, keyed off a new `CREDENTIALS_ENCRYPTION_KEY` env var — do not derive it from `AUTH_SECRET`, which has its own rotation lifecycle) and route all future `credentials` reads/writes through it. Unit tests: round-trip encrypt/decrypt, tampered-ciphertext rejection, and a fail-closed check when `CREDENTIALS_ENCRYPTION_KEY` is unset. Security sign-off required before merge per `agents/security.md`. (No connector code yet — that's `PaytronixConnector`/`PancherosConnector`, queued separately once this lands.)
- [x] Extend `GET /api/points/history` tests to cover pagination params — `tests/unit/api-points.test.ts` has tests for auth guarding and `?accountId=` scoping but no test verifies that `?limit=` and `?offset=` values are forwarded to `db.pointsHistory.findMany({ take, skip })`; that `limit > 200` is capped at 200 (the `parseIntParam` max arg); or that the response shape always includes `{ history, total, limit, offset }`. Add three cases: custom limit/offset are passed through; limit above 200 clamps to 200; response shape includes all four fields.
- [x] Direct unit tests for `correctOcrDigits` in `src/lib/ocr.ts` — `correctOcrDigits` is exported but only implicitly tested through `extractPointsFromText`. Its boundary conditions (correction only within numeric runs; standalone letters in words like "Points" must not be mangled; multi-character runs like `O,24O` → `0,240`; combined confusions `IlSB`) have no direct assertions. Add a `describe("correctOcrDigits")` block in `tests/unit/ocr-patterns.test.ts` covering: each letter-for-digit substitution (`O → 0`, `I/l → 1`, `S → 5`, `B → 8`); a standalone word like "Points" is unchanged; comma-separated numbers with OCR artifacts; mixed known-good + OCR-corrupted runs in the same string.
- [x] Architect: design a cleanup strategy for unbounded `SignupAttempt` rows — `SignupAttempt` rows are inserted on every signup attempt (including blocked/rate-limited ones) but are never deleted; over time the table grows unboundedly and the `@@index([ipHash, createdAt])` TTL window queries slow down. Survey options — (a) delete rows older than 2h in the existing `/api/cron/scrape-deals` run (piggyback onto an existing cron, no `vercel.json` change), (b) a dedicated lightweight `DELETE FROM SignupAttempt WHERE createdAt < now() - interval '2 hours'` cron step — and record the chosen approach + rejection reasoning in `docs/DECISIONS.md`. Queue a follow-up implementation task.
- [x] Wire `AUTH_SECRET` into Playwright's dev server so credential-login e2e tests stop failing — added `AUTH_SECRET: "playwright-e2e-test-secret-do-not-use-in-prod"` to `playwright.config.ts`'s `webServer.env` so NextAuth can sign/verify session JWTs in the dev server. No `.env*` files touched.
- [x] Add Content Security Policy header to `next.config.ts` — the headers array already ships `X-Content-Type-Options`, `X-Frame-Options`, `Referrer-Policy`, and `Permissions-Policy` but no `Content-Security-Policy`; add a CSP suitable for Next.js App Router (at minimum `default-src 'self'`, allow `'unsafe-inline'` on `style-src` for Tailwind, allow the Vercel analytics/speed-insights script domain if used). Verify no existing inline scripts or external font/image sources break by checking `next.config.ts`, layout files, and the CSP in browser DevTools locally.

- [x] Rate-limit NextAuth credentials login attempts — `LoginAttempt` model (ipHash, emailHash, createdAt) added via migration `20260706000000_add_login_attempt`; `src/lib/auth-login-rate-limit.ts` provides `hashEmail`, `isLoginRateLimited`, and `recordFailedLogin` helpers; `src/lib/auth.ts`'s `authorize()` now hashes the IP (via second `request` param) and email, rejects with `null` when either cap (10 failures / 15 min) is hit, and records a `LoginAttempt` row on every bad-password or unknown-user failure. Covered by `tests/unit/auth-login-ratelimit.test.ts` (under-cap, at-cap by IP, at-cap by email, window scoping, create args, hash-not-raw-value checks).

- [x] Architect: decide how credentials login should work under MSW/no-DB e2e mode — decision recorded in `docs/DECISIONS.md` (2026-07-03): Option A (MSW-aware bypass in `authorize()` with server-only `AUTH_BYPASS_FOR_PLAYWRIGHT` env var + `NODE_ENV !== "production"` guard) selected over Option B (real Postgres for Playwright). Implementation task queued at top of Queue.
- [x] Replace manual field validation in `POST /api/auth/signup` with Zod — `src/app/api/auth/signup/route.ts` already uses `z.object({ email: z.string().email(), password: z.string().min(8, ...), name: z.string().min(1).max(80).optional() }).safeParse(body)` and returns 400 on failure; `tests/unit/api-auth-signup.test.ts` covers invalid-email → 400 and short-password → 400. Confirmed already done; no additional code change needed.
- [x] Un-fixme `"unauthenticated user is redirected to login"` in `tests/e2e/auth.spec.ts` — `test.fixme` removed; assertion updated to also confirm `url.searchParams.get("from") === "/dashboard"` (middleware.ts sets `?from=<pathname>` on every `/dashboard/**` redirect, verified by code review of `middleware.ts:14-18`). `AUTH_SECRET` also wired into `playwright.config.ts`'s `webServer.env` (same value as PR #126; whichever merges second resolves a trivial one-line conflict).
- [x] Security: fix pre-account-takeover via unverified-email account linking — landed via PR #98: `User.emailVerified DateTime?` (migration `20260627050000_add_user_email_verified`), `src/lib/auth-link.ts`'s `linkGoogleAccount` refuses to auto-link a Google identity onto an existing row with an unverified email, links cleanly onto verified rows, and stamps `emailVerified` on brand-new Google sign-ins; covered by `tests/unit/auth-link.test.ts`. (This entry was mistakenly resurrected into the Queue by the `[routine] groom backlog (#104)` PR, which branched from a base that predated #98's merge; reconciled here.)
- [x] Affordable-redemption-alert: cron route — `GET /api/cron/affordable-redemptions` (`src/app/api/cron/affordable-redemptions/route.ts`) landed via PR #100; cron-secret guarded, wires `affordable-alerts.ts` + `affordable-alert-email.ts`, records `AffordabilityAlert` rows, registered in `vercel.json`; covered by `tests/unit/api-affordable-redemptions-cron.test.ts`. (Also mistakenly resurrected by PR #104; reconciled here.)
- [x] Affordable-redemption-alert: Settings UI toggle — landed via PR #101: `src/app/dashboard/settings/page.tsx`'s `PrefKey` union includes `notifyAffordable`, wired to an "Affordable redemption alerts" switch; covered by `tests/unit/settings-page-affordable-toggle.test.tsx`. (Also mistakenly resurrected by PR #104; reconciled here.)
- [x] Playwright E2E for the redeem flow — landed via PR #102: `tests/e2e/redeem-flow.spec.ts` signs in (seeded/MSW), marks a redemption, confirms the dialog, and asserts the balance/affordability update. Also fixed a latent `GET /api/redemptions` mock-handler bug (missing nested `chain` object) that silently broke the redeem page under MSW dev mode. (The PR #104 blocked-note referencing an in-flight `routine/redeem-flow-e2e` branch was stale by the time #104 merged; reconciled here.)
- [x] Playwright E2E for the OCR screenshot upload flow — landed via PR #103: `tests/e2e/screenshot-upload.spec.ts` uploads a fixture screenshot via `AddAccountModal`, confirms the OCR-extracted balance pre-fill, and asserts the linked account's displayed balance. (The PR #104 blocked-note referencing an in-flight `routine/ocr-upload-e2e` branch was stale by the time #104 merged; reconciled here.)
- [x] Add per-test isolation for the MSW dev-mode mock store used by Playwright e2e — `resetMockStore()`'s `accounts`/`dealsList`/`redemptions`/`pointsHistory` state lives entirely inside whatever JS realm runs `tests/mocks/handlers.ts` (the browser Service Worker in MSW dev mode, or the Vitest Node process); a real Next.js server route could never reach it, since MSW intercepts client `fetch()` calls before they leave the browser. Implemented as a new MSW handler instead — `POST /api/test/reset-mock-store` in `tests/mocks/handlers.ts`, intercepted by the same SW/worker as everything else and inherently dev/test-only since it only loads when MSW is started. Added `tests/e2e/reset-mock-store.ts`'s `resetMockStore(page)` helper (calls it via `page.evaluate(() => fetch(...))`, not `page.request`, which bypasses the SW; reloads afterward so in-flight initial fetches don't race the reset) and wired it into `deals-feed.spec.ts`, `redeem-flow.spec.ts`, and `screenshot-upload.spec.ts`'s `beforeEach`/first-navigation. Verified empirically with a standalone Playwright probe that fresh `BrowserContext`s (Playwright's per-test default) already don't share mock-store state — this hardens the one real risk case: specs that deliberately share a context across tests, like `add-account.spec.ts`'s `serial` mode. Left `add-account.spec.ts`/`dashboard.spec.ts` unwired here since their real-UI rewrite lives on the still-open `routine/rewrite-account-e2e-specs` branch (PR #106) — wire the same helper in there once it merges.
- [x] Architect: design which remaining stub connector graduates to a real API integration first — surveyed `pancheros`/`pandaexpress`/`kfc`/`culvers`/`buffalowildwings` (the 5 chains with no scrapeable deals source) on vendor platform, auth shape, and documented blockers; picked `pancheros` (Paytronix, shared multi-tenant vendor, no bot-wall blocker, simpler session-cookie auth) over the other 4, which all carry either an Akamai bot-wall (`buffalowildwings`, inherited from Dunkin') or an explicit "unverified" caveat. Full survey table + 7-step implementation decomposition recorded in `docs/DECISIONS.md`'s 2026-06-29 entry; the first decomposition step (encrypt `Account.credentials` at rest) is now queued above as its own task.
- [x] Docs: update the "9 chains" copy in `docs/PROJECT.md` — `CHAINS` in `src/lib/constants.ts` has grown to 17 entries; both the intro line and the `db:seed` comment now say "17 chains" (full chain list spelled out in the intro), with an inline comment pointing at `Object.keys(CHAINS).length` as the source of truth so this is easy to re-derive next time a chain is added.
- [x] Fix stale fixture dates in `tests/mocks/fixtures/deals.json` — all `expiresAt` values advanced from May/June 2026 to 2027 so fixture deals render as active (not "Expired") in dev and Playwright e2e; `scrapedAt` values updated to 2026-07-01 to reflect the refresh.
- [x] Unit tests for `timeUntil` in `src/lib/utils.ts` — `tests/unit/utils.test.ts` now imports and tests `timeUntil` with 7 cases using `vi.useFakeTimers()`: past dates → "Expired", exact-moment → "Expired", days/hours/minutes → correct label, sub-minute clamped to "1m", string input same as Date.
- [x] Extend `POST /api/accounts` tests for invalid `currentPoints` — added 3 cases to `tests/unit/api-accounts.test.ts`: negative integer, non-integer float, and non-numeric string all return 400 and do not call `db.account.create` (Zod schema `z.number().int().nonnegative()` is now exercised directly on the POST path, mirroring the PUT coverage).
- [x] Add Next.js `middleware.ts` auth guard — unauthenticated requests to `/dashboard/**` are redirected to `/login?from=<path>`; authenticated requests to `/login`, `/signup`, and `/reset-password` are redirected to `/dashboard`. Live on `main`.
- [x] Add bespoke Cheerio scraper for `dairyqueen` — `src/lib/scrapers/dairyqueen.ts` (`DairyQueenScraper extends BaseScraper`) targeting `news.dairyqueen.com`, registered in `scrapers` map in `src/lib/scrapers/index.ts`, tested in `tests/unit/scrapers/dairyqueen.test.ts` (fixture-HTML parse + network-error-returns-empty-array).
- [x] Docs: update "9 chain scrapers" count in `docs/ARCHITECTURE.md` — updated to 10 (chipotle was added since that copy was written); noted that the count drifts and points to `src/lib/scrapers/index.ts` as the authoritative list; also pruned stale "Known gaps" entries (CI and redeem E2E are done).
- [x] Unit tests for `middleware.ts` — `tests/unit/middleware.test.ts` added with 6 cases: unauthenticated `/dashboard` → redirect to `/login?from=%2Fdashboard`; unauthenticated `/dashboard/settings` → redirect with `from`; authenticated `/login` → redirect to `/dashboard`; authenticated `/signup` → redirect to `/dashboard`; unauthenticated `/` → passes through; authenticated `/dashboard` → passes through. Mock `auth` from `@/lib/auth` as `(handler) => handler` so the middleware default export is called directly with a controlled `req.auth`.
- [x] Affordable-redemption-alert: schema migration — `User.notifyAffordable Boolean @default(true)` and the `AffordabilityAlert` model (`{ id, accountId, userId, redemptionOptionId, sentAt }`, `@@unique([accountId, redemptionOptionId])`, `@@index([userId])`, cascading FKs) landed via migration `20260620151332_add_affordability_alert`; `prisma/seed.ts` remains idempotent.
- [x] Affordable-redemption-alert: detection helpers — `src/lib/affordable-alerts.ts`'s `findNewlyAffordableAccounts`/`affordableAlertKey`, reusing `bestRedemptionFor`; covered by `tests/unit/affordable-alerts.test.ts` (not-yet-affordable, newly-affordable, already-alerted cases).
- [x] Affordable-redemption-alert: email — `src/lib/affordable-alert-email.ts`'s `sendAffordableRedemptionEmail`, mirroring `deal-reminder-email.ts`'s Resend send-or-log-in-dev pattern; covered by `tests/unit/affordable-alert-email.test.ts`.
- [x] Architect: design an "affordable redemption alert" feature — recorded in `docs/DECISIONS.md`'s 2026-06-19 entry; implementation tasks queued (3 of the 4 — schema/detection/email — are now also done; the cron route + Settings toggle remain in the Queue above with their stale "PR #81 open/unmerged" blocked-notes removed since that schema PR has since merged).
- [x] Implement `POST /api/accounts/[id]/redeem` — `src/app/api/accounts/[id]/redeem/route.ts` verifies account ownership (404), chain match (400), sufficient points (400), then atomically deducts `pointsCost` and inserts a `PointsHistory` row (`changeReason: "REDEMPTION"`) in a `db.$transaction`; covered by `tests/unit/api-accounts-redeem.test.ts`.
- [x] DX: add a Husky + lint-staged pre-commit hook — `.husky/pre-commit` runs `npx lint-staged` then `npm run typecheck`; documented in `docs/DEPLOYMENT.md`.
- [x] Unit tests for `mapScrapedDeal` and `deactivateExpiredDeals` in `src/lib/deals.ts` — covered in `tests/unit/deals.test.ts`.
- [x] Unit tests for `hashResetToken`, `mintResetToken`, and `resetTokenExpiry` in `src/lib/reset-tokens.ts` — `tests/unit/reset-tokens-lib.test.ts`.
- [x] Unit tests for `bestRedemptionFor` and `bestRedemptionLabel` in `src/lib/dashboard.ts` — `tests/unit/dashboard-best-redemption.test.ts`.
- [x] Unit tests for `dealTypeLabel` and `discountTypeLabel` in `src/lib/formatters.ts` — `tests/unit/formatters.test.ts`.
- [x] API integration tests for `PUT /api/points/update` — `tests/unit/api-points-update.test.ts`.
- [x] Unit tests for `sendResetEmail` in `src/lib/reset-tokens.ts` — covered across `tests/unit/api-auth-password.test.ts` and `tests/unit/reset-tokens-lib.test.ts`.
- [x] Accessibility: add `aria-label` to icon-only buttons in `ChainAccountCard` and nav components — `ChainAccountCard.tsx` (Edit points/Save/Cancel/Open rewards page/See all redemptions/Sync now/Account settings), `Sidebar.tsx` (Toggle theme/Sign out/Expand-Collapse sidebar), and `MobileNav.tsx` (Menu) all have descriptive `aria-label`s; regression-tested in `tests/unit/icon-button-accessibility.test.tsx`. (`DealCard`'s button has visible text, not icon-only — no label needed there.)
- [x] Add stub `BaseConnector` entries for the 7 chains missing from the connectors registry — `pancheros`, `dairyqueen`, `culvers`, `jimmyjohns`, `buffalowildwings`, `kfc`, and `pandaexpress` all resolve via `getConnector`; `tests/unit/connector-registry.test.ts` parametrizes over every `CHAINS` key so a future un-registered chain fails the suite.
- [x] Add OCR point-pattern regexes for chains using the generic fallback — `chipotle`, `pancheros`, `dairyqueen`, `culvers`, `jimmyjohns`, `buffalowildwings`, `kfc`, and `pandaexpress` all have chain-specific entries in `POINTS_PATTERNS` (`src/lib/ocr.ts`).
- [x] Add a bespoke Cheerio scraper for `chipotle` — `src/lib/scrapers/chipotle.ts`, registered in `src/lib/scrapers/index.ts`, tested in `tests/unit/scrapers/chipotle.test.ts`.
- [x] Migrate off deprecated `next lint` — `package.json`'s `lint` script is now `"eslint ."`.
- [x] Fix per-chain error isolation in deal-scraping routes — shared scan-and-replace loop isolates each chain's `replaceAutoDeals` failure so one chain's error doesn't abort the rest.
- [x] Return 400 (not 500) for a corrupt or spoofed image upload in `POST /api/upload` — `src/app/api/upload/route.ts` catches the `sharp()` decode call specifically and returns 400 ("Invalid image data").
- [x] Add standard security response headers — `next.config.ts` ships `X-Content-Type-Options`, `X-Frame-Options`, `Referrer-Policy`, and `Permissions-Policy` on all routes.
- [x] Bring `docs/API.md` up to date with the real route list — all 23 routes documented.
- [x] Add `aria-current="page"` to the active dashboard nav link — both `Sidebar.tsx` and `MobileNav.tsx`.
- [x] Unit tests for `replaceAutoDeals` in `src/lib/deals.ts` — mocks `db.deal.deleteMany`/`createMany`/`$transaction` in `tests/unit/deals.test.ts`; asserts the chain/auto-source scoping, `deleteMany` runs before `createMany`, zero deals still clears existing auto deals, and the return value equals the row count.
- [x] Rate-limit `POST /api/auth/signup` by IP — added a `SignupAttempt` Prisma model (hashed IP + `createdAt`, via migration `20260620153316_add_signup_attempt`), `getClientIp`/`hashClientIp` helpers in `src/lib/api.ts`, and a 5-per-IP-per-hour cap returning 429 in `src/app/api/auth/signup/route.ts`; extended `tests/unit/api-auth-signup.test.ts` with under-cap/at-cap/scoping/hash cases.
- [x] Add CI workflow `.github/workflows/ci.yml` — `npm ci`, `npm run typecheck`, `npm run lint`, `npm run test:run` on PRs to main; green on default branch.
- [x] OCR edge-case tests in `src/lib/ocr.ts` — common OCR artifacts are corrected per chain; malformed input fails gracefully (no throw).
- [x] API auth/cron guard tests — `requireAuth` rejects unauthenticated calls; `isCronRequest` only accepts the configured cron secret.
- [x] Cents-per-point ranking unit tests — given balances + redemption options, the highest cents-per-point redemption is surfaced first; ties and zero-point cases handled.
- [x] Unit tests for the connectors registry (`src/lib/connectors/`) — `getConnector` returns the right connector per chain slug and `undefined` for unknown slugs; `hasImplementedConnector` reflects each connector's `implemented` flag; every unimplemented connector's `authenticate`/`getPointsBalance`/`getRecentTransactions`/`refreshToken` reject with `NotImplementedError`.
- [x] Harden `isCronRequest` against an unset `CRON_SECRET` — rejects empty/undefined `CRON_SECRET` regardless of header value; regression coverage in `tests/unit/api-guards.test.ts`.
- [x] Fix lint warnings in `src/lib/connectors/base.ts` — eslint-disable scoped to the stub file's intentionally-unused parameters; `npm run lint` is clean.
- [x] API tests for `/api/accounts/[id]` edge cases — `PUT`/`DELETE` return 404 (not 403) for a cross-user account; invalid/non-numeric `currentPoints` rejected with 400; per-user scoping verified in `tests/unit/api-accounts.test.ts`.
- [x] Unit tests for `src/lib/deal-reminder-email.ts` — subject/body rendering for 0, 1, and many upcoming-expiry deals (pluralization), and multi-chain digests, in `tests/unit/deal-reminder-email.test.ts`.
- [x] Unit tests for `src/lib/extension-auth.ts` and `src/lib/extension-bridge.ts` — pairing-token issuance and rejection of malformed/revoked tokens in `tests/unit/extension-auth-unit.test.ts` and `tests/unit/api-extension-pair.test.ts`.
- [x] Unit tests for `src/lib/points-history.ts` — `buildBalanceSeries` covered for empty input, chronological ordering, single-change anchoring, and no-mutation in `tests/unit/points-history-series.test.ts`.
- [x] Edge-case unit tests for `src/lib/deal-reminders.ts` — exact reminder-cutoff boundary, missing `expiresAt`, and already-reminded (no duplicate) cases in `tests/unit/deal-reminders.test.ts`.
- [x] Fix 403→404 existence-disclosure in `GET /api/points/history?accountId` — uses `db.account.findFirst({ where: { id, userId } })`; regression test in `tests/unit/api-points.test.ts` verifies another user's `accountId` returns 404, not 403.
- [x] `requireAdmin` unit tests — 401 with no session, 403 for a non-admin email, fail-closed 403 for an empty/unset `ADMIN_EMAIL`, case-insensitive match, and comma-separated-list support, in `tests/unit/api-guards.test.ts`.
- [x] Unit tests for `mintExtensionToken` and `hashExtensionToken` in `src/lib/extension-auth.ts` — `tests/unit/extension-auth-lib.test.ts`.
- [x] Unit tests for `verifyUnsubscribeToken` in `src/lib/unsubscribe-token.ts` — round-trip, wrong-secret, truncated, empty, and tampered-payload cases in `tests/unit/unsubscribe-token.test.ts`.
- [x] Unit tests for `parseIntParam` in `src/lib/api.ts` — `tests/unit/parse-int-param.test.ts`.
- [x] Unit tests for `estimatedDealValueCents` and `totalEstimatedDollars` in `src/lib/dashboard.ts` — `tests/unit/dashboard-deal-value.test.ts`.
- [x] API integration test for `GET /api/cron/deal-reminders` — cron-secret guard, zero-candidate stats shape, and per-user send errors captured without aborting the batch, in `tests/unit/api-deal-reminders-cron.test.ts`.
- [x] Unit tests for `affordableDealCount` in `src/lib/dashboard.ts` — `tests/unit/dashboard-affordable-count.test.ts`.
- [x] API integration test for `GET /api/cron/scrape-deals` — cron-secret guard, per-chain error isolation, and `chainsScanned`/`dealsInserted`/`dealsDeactivated` in the response, in `tests/unit/api-cron-scrape-deals.test.ts`.
- [x] CI: add `prisma generate` step before typecheck and tests in `.github/workflows/ci.yml` so fresh CI runs don't fail with `Module '"@prisma/client"' has no exported member` errors.
- [x] Rate-limit password-reset email sends — `POST /api/auth/forgot-password` skips minting/sending (still returns `{ ok: true }`) once a user has 3+ tokens created in the last hour; tests in `tests/unit/api-auth-password.test.ts`.
- [x] Replace the native `window.confirm()` in `AccountDetailsDialog`'s `disconnect()` with the app's own `Dialog` component — added a `confirmingDisconnect` state that swaps the same `<Dialog>`'s content to a focused confirm view (chain name, "You can always relink later." copy, Cancel/Disconnect buttons) before calling `DELETE /api/accounts/:id`, mirroring `AddAccountModal`'s multi-step-within-one-Dialog convention rather than nesting a second Radix Dialog inside the already-open one. Covered by the new `tests/unit/account-details-dialog-disconnect.test.tsx` (confirm step appears, Disconnect-again triggers the real delete via MSW, Cancel backs out without deleting); verified live against `npm run dev` (MSW) that no native dialog event fires and the confirm/cancel flows both work. Deferred: un-fixme-ing `tests/e2e/add-account.spec.ts`'s `"shows confirmation when deleting an account"` test — that file's rewrite (this branch's base predates it) lives on the separate, still-unmerged `routine/rewrite-account-e2e-specs` PR; un-fixme it there (or in a follow-up) once that PR lands.
- [x] Rewrite `tests/e2e/add-account.spec.ts` and the broken parts of `tests/e2e/dashboard.spec.ts` against the real `/dashboard` UI — both specs assumed a standalone `/dashboard/accounts` route and a standalone "Unlink account" button, neither of which exists; account linking/unlinking lives directly on `/dashboard` via `AddAccountModal` (link) and `ChainAccountCard`'s "Account settings" button → `AccountDetailsDialog`'s "Disconnect" action (unlink, gated behind a native `window.confirm()`). Rewrote `add-account.spec.ts` in full and the "navigates between dashboard pages via sidebar" + "shows empty state when no accounts linked" tests in `dashboard.spec.ts` to match the real UI (sidebar's actual 4th link is Settings, not Accounts; the real empty-state copy is "Link your first chain to start stacking."); left the "shows account cards"/"displays total estimated value" tests and the whole "affordability flow" describe block untouched, and kept `test.fixme("shows confirmation when deleting an account")` fixme'd since it depends on the in-app confirm dialog the next task adds. Verified locally against a live `npm run dev` (MSW) server: typecheck clean, lint clean, `npm run test:run` 602/602, and all 8 targeted Playwright tests passing (4 per spec, plus the fixme correctly skipped). Note: the pre-existing "affordability flow" tests fail independently of this change — `tests/mocks/fixtures/deals.json`'s `expiresAt` dates are all hardcoded to May 2026 and have since drifted into the past; queued separately for grooming.
- [x] Add a bespoke Cheerio scraper for `jimmyjohns` — `src/lib/scrapers/jimmyjohns.ts`, mirroring `chipotle.ts`'s best-effort-selectors-with-mock-fallback pattern, registered in `src/lib/scrapers/index.ts`'s `scrapers` map; updated the now-stale "7 chains with no bespoke scraper" comment in `src/lib/scrapers/sources.ts` to 6 and noted jimmyjohns now has the same dual LLM-then-Cheerio-fallback path as chipotle. Covered by `tests/unit/scrapers/jimmyjohns.test.ts` (fixture-HTML parse + network-error-returns-empty cases, mirroring `chipotle.test.ts`).
