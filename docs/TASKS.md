# PointStash — Task Queue

The **daily routine** takes the **top unblocked** unchecked task, implements it on a
`routine/<slug>` branch, and opens a PR.

Format: `- [ ] <title> — <acceptance criteria>`
- Top = highest priority. A `> blocked: <reason>` line skips a task until cleared.
- The routine ticks `[x]` inside its PR.

## Queue

- [ ] Playwright E2E for the redeem flow — sign in (seeded/MSW), view a deal, complete a redeem, see the balance update.
  > blocked: no in-app "complete a redeem" action exists yet — the Redeem CTA on `DealCard` (`src/components/dashboard/DealCard.tsx`) just opens an external link in a new tab; there's no balance-deduction flow to test. Needs architect design of a redemption-completion feature (see the design task below) before this E2E can be written.
- [ ] Harden `isCronRequest` against an unset `CRON_SECRET` — `src/lib/api.ts` builds `Bearer ${process.env.CRON_SECRET}`, so if the env var is ever unset a literal `Authorization: Bearer undefined` header passes. Reject when `CRON_SECRET` is empty/undefined regardless of header value; add a regression test in `tests/unit/api-guards.test.ts`.
- [ ] Architect: design a redemption-completion flow — decompose "user marks a redemption option as redeemed, the linked account's points balance is deducted by `pointsCost`, and a `PointsHistory` entry is recorded" into implementable tasks (schema check, API route, UI affordance on `RedeemPage`/`DealCard`). Record the design in `docs/DECISIONS.md` and leave a follow-up implementation task at the top of the queue. Unblocks the redeem-flow E2E above.
- [ ] Fix lint warnings in `src/lib/connectors/base.ts` — resolve the 4 `@typescript-eslint/no-unused-vars` warnings on the `_credentials`/`_token` stub parameters (e.g. an eslint override for this file, or restructure the stub signatures) without changing `BaseConnector`'s public contract; `npm run lint` reports zero warnings on this file.
- [x] Unit tests for the connectors registry (`src/lib/connectors/`) — `getConnector` returns the right connector per chain slug and `undefined` for unknown slugs; `hasImplementedConnector` reflects each connector's `implemented` flag; every unimplemented connector's `authenticate`/`getPointsBalance`/`getRecentTransactions`/`refreshToken` reject with `NotImplementedError`.
- [ ] API tests for `/api/accounts/[id]` edge cases — `PUT`/`DELETE` return 404 (not 403, to avoid leaking existence) for an account belonging to another user; invalid/non-numeric `currentPoints` is rejected with 400; verifies per-user scoping in the underlying query.
- [ ] Unit tests for `src/lib/deal-reminder-email.ts` — subject/body rendering for 0, 1, and many upcoming-expiry deals (pluralization), and deals across multiple chains in one digest.
- [ ] Unit tests for `src/lib/extension-auth.ts` and `src/lib/extension-bridge.ts` — pairing-token issuance, expiry, and rejection of malformed/expired/already-used tokens.
- [ ] Unit tests for `src/lib/points-history.ts` — series aggregation across a date range with gaps (no recorded points on some days), multiple accounts for the same user, and an account with a single data point.
- [ ] Edge-case unit tests for `src/lib/deal-reminders.ts` — a deal expiring exactly at the reminder cutoff boundary, a deal with no `expiresAt`, and a deal that already has a reminder recorded (no duplicate send).
- [ ] Accessibility pass on icon-only dashboard buttons — audit `ChainAccountCard`, `DealCard`, and `Sidebar`/`MobileNav` for icon-only buttons missing `aria-label` (e.g. unlink, redeem, nav icons); add labels so each is announced correctly by screen readers, with a test asserting accessible names via `getByRole`.
- [ ] Playwright E2E for the OCR screenshot upload flow — on `/dashboard/accounts`, upload a fixture screenshot via `AddAccountModal`'s screenshot method, confirm the OCR-extracted point total pre-fills the balance field, and saving updates the account's displayed balance.
- [ ] DX: add a Husky + lint-staged pre-commit hook — run `npm run lint` and `npm run typecheck` (or an eslint/tsc subset) on staged files before commit; document setup in `docs/DEPLOYMENT.md` or a new `docs/DX.md`.
- [ ] Fix 403→404 existence-disclosure in `GET /api/points/history?accountId` — the route calls `db.account.findUnique({ where: { id: accountId } })` then returns 403 if `userId` mismatches, leaking that the account exists; change to `db.account.findFirst({ where: { id: accountId, userId: guard.userId } })` returning 404 on null; add regression tests to `tests/unit/api-points.test.ts` verifying that another user's `accountId` returns 404, not 403.
- [ ] `requireAdmin` unit tests — extend `tests/unit/api-guards.test.ts`: returns 401 when there is no session; returns 403 when the session user's email is not in `ADMIN_EMAIL`; returns 403 (fails closed) when `ADMIN_EMAIL` is empty or unset; accepts the correct admin email case-insensitively; accepts one of a comma-separated list of admin emails.
- [ ] Unit tests for `mintExtensionToken` and `hashExtensionToken` in `src/lib/extension-auth.ts` — in new `tests/unit/extension-auth-lib.test.ts`: `mintExtensionToken` in non-production returns a string starting with `ps_dev_`; token contains only URL-safe characters (no `+`, `/`, `=`); `hashExtensionToken` returns a 64-char lowercase hex string; the same input always produces the same hash; two different tokens produce different hashes.
- [ ] Unit tests for `chainHasExtensionSupport` and `EXTENSION_SUPPORTED_CHAINS` in `src/lib/extension-bridge.ts` — in new `tests/unit/extension-bridge-lib.test.ts`: `chainHasExtensionSupport` returns true for each of the 14 slugs in `EXTENSION_SUPPORTED_CHAINS`; returns false for an unknown slug and for empty string; `EXTENSION_SUPPORTED_CHAINS.size` is exactly 14; `isExtensionConfigured` returns false when `NEXT_PUBLIC_EXTENSION_ID` is unset, true when set.
- [ ] Unit tests for `verifyUnsubscribeToken` in `src/lib/unsubscribe-token.ts` — extend `tests/unit/unsubscribe-token.test.ts`: round-trip `verifyUnsubscribeToken(signUnsubscribeToken(userId, secret), secret)` returns the original `userId`; wrong secret returns null; truncated token (missing the dot-separator) returns null; empty string returns null; a tampered payload (changed userId, original signature) returns null.
- [ ] Unit tests for `parseIntParam` in `src/lib/api.ts` — add `tests/unit/parse-int-param.test.ts` (or extend api-guards): valid integer string returns parsed value; negative string falls back to the default; NaN / non-numeric string falls back; null/empty falls back to default; a value above `max` is clamped to `max`; zero is accepted and not treated as falsy.
- [ ] Unit tests for `estimatedDealValueCents` and `totalEstimatedDollars` in `src/lib/dashboard.ts` — in new `tests/unit/dashboard-value-estimates.test.ts`: `estimatedDealValueCents` returns null when `pointsCost` is null; returns null when the chain has no redemptions; returns correct cents for a known rate; `totalEstimatedDollars` sums multiple accounts' values; a zero-balance account contributes 0; an account for a chain with no redemption contributes 0.
- [ ] API integration test for `GET /api/cron/deal-reminders` — in `tests/unit/api-deal-reminders-cron.test.ts` (extend if it exists): cron request without a valid `CRON_SECRET` header → 401; with a valid header and no opted-in users → `{ ok: true, candidates: 0, sent: 0, logged: 0, errors: [] }`; a per-user email-send error is captured in `errors[]` without aborting the batch (other users still get emailed).
- [ ] Implement `POST /api/accounts/[id]/redeem` — per the DECISIONS.md redemption-completion design: verify account ownership (404, not 403), verify the `RedemptionOption` belongs to the same chain (400), verify the account has enough points (400), then atomically deduct `pointsCost` from `currentPoints` and insert a `PointsHistory` entry with `changeReason: "REDEMPTION"`; unit tests in `tests/unit/api-accounts-redeem.test.ts` cover the 404/400 guards and the happy-path atomic update; unblocks the redeem-flow E2E.
- [ ] Unit tests for `mapScrapedDeal` and `deactivateExpiredDeals` in `src/lib/deals.ts` — `mapScrapedDeal` maps optional fields to null, always sets `source: "LLM"` and `isVerified: false`, `isActive: true`; `deactivateExpiredDeals` mocks db and asserts `updateMany` receives `{ isActive: true, expiresAt: { lt: now } }` and returns the count; create `tests/unit/deals.test.ts`.
- [ ] Unit tests for `replaceAutoDeals` in `src/lib/deals.ts` — mock db, verify `$transaction` receives `[deleteMany(chainId, AUTO_SOURCES), createMany(rows)]` in that order; zero scraped deals still clears existing auto deals; return value equals the number of deals passed in; extend `tests/unit/deals.test.ts`.
- [ ] Unit tests for `hashResetToken`, `mintResetToken`, and `resetTokenExpiry` in `src/lib/reset-tokens.ts` — in `tests/unit/reset-tokens-lib.test.ts`: `hashResetToken` returns 64-char lowercase hex; same input always produces the same hash; different inputs differ; `mintResetToken` returns a URL-safe base64 string with no `+`, `/`, or `=`; `resetTokenExpiry` returns a Date approximately 1h in the future (within ±5s); `RESET_TOKEN_TTL_MS` is 3 600 000 ms.
- [ ] Unit tests for `bestRedemptionFor` and `bestRedemptionLabel` in `src/lib/dashboard.ts` — in `tests/unit/dashboard-best-redemption.test.ts`: `bestRedemptionFor` returns null for empty list; returns the single option if only one; returns the highest centsPerPoint among several; ignores options for other chains; `bestRedemptionLabel` returns "Linking redemptions…" when null; formats `itemName · pointsCost pts` for a valid option (locale-formatted number).
- [ ] Unit tests for `affordableDealCount` in `src/lib/dashboard.ts` — extend `tests/unit/dashboard-affordable-count.test.ts` or create a companion file: deals without a pointsCost are excluded; deals where balance < pointsCost are excluded; deals where balance >= pointsCost are counted; multiple accounts, multiple deals across chains; a chain with no linked account counts as unaffordable.
- [ ] API integration test for `GET /api/cron/scrape-deals` — in `tests/unit/api-cron-scrape-deals.test.ts` (extend if it exists): 401 without cron secret; `deactivateExpiredDeals` is called before scraping; a chain scrape error is recorded in errors[] without aborting other chains; all chain scrape results are returned in the JSON body with `chainsScanned` count.
- [ ] CI: add `prisma generate` step before typecheck and tests in `.github/workflows/ci.yml` so fresh CI runs don't fail with `Module '"@prisma/client"' has no exported member` errors; the generate step should use `npx prisma generate` without a database URL (schema → client only).
- [ ] Unit tests for `dealTypeLabel` and `discountTypeLabel` in `src/lib/formatters.ts` — in `tests/unit/formatters.test.ts`: every key in `dealTypeLabel` maps to a non-empty string; every key in `discountTypeLabel` maps to a non-empty string; badge variant maps return one of the expected union string values; coverage is exhaustive for all keys defined in the record (no silent gaps).
- [ ] API integration tests for `PUT /api/points/update` — in `tests/unit/api-points-update.test.ts` (extend if it exists): 401 without a session; 404 when the account doesn't belong to the authenticated user; valid update returns 200 and inserts a `PointsHistory` row; verify `findFirst({ where: { id, userId } })` is used (no cross-user leakage).
- [ ] Unit tests for `sendResetEmail` in `src/lib/reset-tokens.ts` — mock Resend; in non-production with `RESEND_API_KEY` unset, the function logs the reset link and returns (no throw); in production with no key, it throws `"RESEND_API_KEY is not configured"`; Resend send returning `{ error }` throws with the error message; happy path calls Resend with correct `to`, `from`, `subject`, and `html`/`text` containing the reset link.
- [ ] Accessibility: add `aria-label` to icon-only buttons in `ChainAccountCard`, `DealCard`, and nav components — audit with a Vitest + `@testing-library/react` test asserting `getByRole("button", { name: /unlink|redeem|menu/i })` passes; `aria-label` text should be descriptive enough for a screen-reader user to understand the action without visual context.

## Done
<!-- routine PRs move completed items here -->

- [x] Add CI workflow `.github/workflows/ci.yml` — `npm ci`, `npm run typecheck`, `npm run lint`, `npm run test:run` on PRs to main; green on default branch.
- [x] OCR edge-case tests in `src/lib/ocr.ts` — common OCR artifacts are corrected per chain; malformed input fails gracefully (no throw).
- [x] API auth/cron guard tests — `requireAuth` rejects unauthenticated calls; `isCronRequest` only accepts the configured cron secret.
- [x] Cents-per-point ranking unit tests — given balances + redemption options, the highest cents-per-point redemption is surfaced first; ties and zero-point cases handled.
- [x] Unit tests for the connectors registry (`src/lib/connectors/`) — `getConnector` returns the right connector per chain slug and `undefined` for unknown slugs; `hasImplementedConnector` reflects each connector's `implemented` flag; every unimplemented connector's `authenticate`/`getPointsBalance`/`getRecentTransactions`/`refreshToken` reject with `NotImplementedError`.
